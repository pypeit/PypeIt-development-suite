# PypeIt Status

# Check state 

## Goals:  Modify the main PypeIt run to run through the code as if it were the full run, but only record the status of the reduction.


### Prompts


#### I wish to modify the code in PypeIt/pypeit/pypeit.py to record the current status of the reduction using the state.py module.  We will begin with just the calibration steps and the calib_all() method.  Please examine pypeit.py and calibrations.py and state.py and provide a plan for the modifications.

#### More specifically, I wish to have the code check whether the expected output files exist and set the state accordingly.  And either way, not perform the reduction step.

#### 

#### I think the existing methods like bias_state(), wvcalib_state(), tilts_state(), and slits_state() in calibrations.py should be moved to the state.py module.  Please examine the code and provide a plan for the modifications.

# Required calibrations

## Goal:  Assess which files are expected to be generated by the reduction process. 

### We are going to generate code to determine which reduction steps are required for the given PypeIt file.

### Begin by adding a new "required" item for the BaseCalibState class to be whether this step is required. It should be a boolean.

### Now generate a method named "required_calibs" in calibrations.py that will modify an input state object to indicate which calibration steps are required for the given PypeIt file.  Base it on the check_for_calibs() method in calibrations.py.  The method should return the state object with the required steps set.

# Isolate state routines

## This didn't go well, so I reverted..

## Goal:  Refactor so that most of the state routines are isolated in the state.py module.

### I would like to move routines like bias_state(), wvcalib_state(), tilts_state(), and slits_state() in calibrations.py to the state.py module so that they can be run outside of the calibration class.  This will allow us to check the status of the reduction process without actually running the reduction.  Can you plan this refactoring?

# Script me

## Generate a script based on run_pypeit.py that will run through the code as if it were the full run, but only record the status of the reduction.  For now, we will only run the calibration steps.  Therefore, it should call the calib_all() method with status_only=True and reload_only=True.  Have the script print the state to the screen in a pretty format.  The script should be called pypeit_status.py.