# Develop the INT/IDS spectrograph

## Goals

We wish to develop the INT/IDS spectrograph for PypeIt.  

## Claude

### Skills

Consider using the skills in PypeIt-development-suite/.claude/skills/

## Context

Examine the following:

    - The INT/IDS website: https://www.ing.iac.es/astronomy/instruments/ids/
    - The raw files in `PypeIt/PypeIt-development-suite/pypeitdev/int_ids/INT_20201218/`

## Coding

Here are guidelines for coding: 

- Use Python where possible
- Add inline comments to explain the effort
- Reuse existing code when possible
- Place import statements at the top of the file.
- Limit lines of code to 80 characters per line
- Include a description of inputs/outputs in the doc string of all methods
- Add "Generated by JXP and Claude" in the doc string of all methods you create

## Testing

If you need to test the code: 

- use the files in `PypeIt/PypeIt-development-suite/pypeitdev/int_ids/INT_20201218/`
- run on the "pypeit14b" conda environment.

## Prompts

1. Read this doc.  Perform the first task under Prep.

## Prep

1.  Check out all of the files in the Context section. We are going to create a new module named `int_ids.py` in the `pypeit/spectrographs/` folder.  Model it after the `mmt_bluechannel.py` module.  Ask me questions in the Q&A sub-section below.  Use Opus.  Log your work in Logs below.

2. See the answers to our first Q&A.  Act accordingly.  Use Opus if you can.  Log your work in Logs below.

3. Please make child classes off of the `INTIDSSpectrograph` class. One for the EEV10 detector and one for the RED+2 detector.  Use Opus.  Log your work in Logs below.

4. Please:

   - Try to run `pypeit_setup` on the main data set.  
   - Write a report of your findings in PypeIt-development-suite/pypeitdev/int_ids/Reports. 
   - If you have any questions, put them in the Q&A section below.  Log your work in Logs below.

5. Thanks for your terrific Report.  I have implemented all of the changes except the Airmass calculation which I am leaving for you to implement. See my answers in the Q&A section below. Please:
   - Use astropy for the calculation following these notes: https://docs.astropy.org/en/latest/coordinates/example_gallery_plot_obs_planning.html
   - Put the method in a common module so it can be reused by other spectrographs.
   - Edit the 02_pypeit_setup.md report to include these updates
   - If you have any questions, put them in the Q&A section below.  
   - Log your work in Logs below.

6.  Excellent work.  Can you now:

   - Generate a PypeIt file
   - Place it in the PypeIt-development-suite/pypeit_files/ folder following the naming convention there
   - Launch the reduction with the pypeit14b environment using `pypeit_test` in PypeIt-development-suite
   - See how far it goes
   - Write a report of your findings in PypeIt-development-suite/pypeitdev/int_ids/Reports.
   - If you have any questions, put them in the Q&A section below.  
   - Log your work in Logs below.

### Q&A

**2026-08-24 (Prep task 1):**

1. **Git branch**: The PypeIt repo was on the `hamspec` branch (clean). Since
   this is new-instrument work, I created a new branch `int_ids` off `develop`
   and did the work there. OK, or would you prefer a different branching setup?
>A. Good call, thanks.

2. **Science vs. standard**: IDS headers type both as `IMAGETYP='object'` /
   `OBSTYPE='TARGET'`. For now I split them by exposure time (standard ≤ 120 s,
   science ≥ 120 s). Is there a better discriminator you'd like (e.g., matching
   `CAT-NAME` against a standard-star list)?
>A. I am ok with 120s

3. **Spectral flip**: I set `specflip=False` (wavelength assumed to increase
   with row number). I'll verify against the CuAr+CuNe arc when we run the
   reduction — do you already know the dispersion direction for EEV10?
>A. I do not, but usually there are more lines to the red

4. **RED+2 camera**: The website says RED+2 (0.44"/pix) is the default camera,
   but the test data use EEV10 (0.40"/pix). I put in a platescale switch on the
   `DETECTOR` card, but I don't know RED+2's gain/readnoise/amp configuration.
   Do you have (or want) RED+2 data in scope for this effort?
>A. Let's only do EEV10 for now, but do have a placeholder for RED+2.

5. **Wavelength calibration**: I set `holy-grail` with lamps
   `['NeI','ArI','ArII','CuI']` (headers say CuAr+CuNe). Longer term, should I
   build a `full_template` archive for R1200B with `pypeit_identify`?
>A. I have put a file named `idarc_bs.ms` in `Projects/PypeIt/PypeIt-development-suite/pypeitdev/int_ids` which contains an IRAF solution for an arc.  Try using that

6. **Report location**: Prompt task 2 says to write reports in
   `pypeitdev/hane_hamspec/Reports` — is that a leftover from the hamspec
   prompt file? Should INT/IDS reports go in `pypeitdev/int_ids/Reports`?
>A. Right, use `pypeitdev/int_ids/Reports` instead.  Fix the prompt too.

7. **Conda env**: The prompt says to test on `pypeit14`, but that env has no
   astropy installed; `pypeit14b` works and imports PypeIt from this checkout,
   so I used it. Should I keep using `pypeit14b`?
>A. Yes, use `pypeit14b`

**2026-08-24 (Prep task 4, `pypeit_setup`):**

8. **Airmass policy**: Report 02 recommends computing the mid-exposure
   airmass with astropy from ra/dec/mjd/site. Should the computed value
   *replace* the header `AIRMASS` for all frames (falling back to the header
   only when coordinates are missing, e.g. bias/flat with RA=DEC=0), or only
   *fill in* when the header card is absent? I lean toward replace-with-
   fallback, since it also validates the header to <0.2%.

9. **Report numbering**: I named the report `02_pypeit_setup.md` (matching
   the name referenced in prompt 5). Should I retroactively write a
   `01_module_creation.md` report covering prompts 1–3, or is the Logs
   section here sufficient?

## Logging

The "Logs" section will record Claude's work.  Please use the following format:

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

...

## Logs

### 2026-08-24 (Ran pypeit_setup; wrote Report 02)

Executed prompt 4. (Model note: session runs on Claude Fable 5.)

- Ran `pypeit_setup -s int_ids_eev10` on
  `pypeitdev/int_ids/INT_20201218/`, both the first pass (`.sorted`) and
  with `-c A` to generate a full `.pypeit` file. **Result: completely
  clean** — 4 files ingested, frame types correct
  (arc,tilt / bias / pixelflat,illumflat,trace / science), one unique
  configuration (R1200B / 4502.182 / EEV10 / 1,1), `.pypeit` and `.calib`
  files written and vetted, zero warnings or errors in the log.
- Both `int_ids_eev10` and `int_ids_redplus2` show up in the script's
  valid-spectrograph list.
- Cross-checked the header `AIRMASS` with an astropy AltAz computation at
  Roque de los Muchachos: agreement <0.2% at both exposure start
  (1.2938 vs AMSTART 1.2930) and mid-exposure (1.3528 vs AIRMASS 1.3549).
- Wrote `pypeitdev/int_ids/Reports/02_pypeit_setup.md` with the full
  results plus five recommended changes: (1) astropy-based mid-exposure
  airmass in a reusable common module (with fallback for RA=DEC=0
  bias/flat frames), (2) dev-suite registration (RAW_DATA staging,
  pypeit_files entry, test_setups.py, test_load_images.py), (3) docs
  (release notes + doc/spectrographs page), (4) frame-typing coverage for
  darks/sky flats/standards when such data exist, (5) flip
  `supported=True` after the dev-suite reduction passes.
- New Q&A items #8 (airmass replace-vs-fill policy) and #9 (whether a
  retroactive Report 01 is wanted).

### 2026-08-24 (Split into per-detector child classes: EEV10 and RED+2)

Executed prompt 3. (Model note: session runs on Claude Fable 5; the model
cannot be switched to Opus mid-session.)

**Restructuring of `pypeit/spectrographs/int_ids.py`** (following the
base/child pattern of `p200_dbsp.py` and `keck_lris.py`):

- `INTIDSSpectrograph` is now an abstract parent (no `name`; not loadable via
  `load_spectrograph('int_ids')` any more). It keeps everything shared:
  metadata mapping, compound meta, frame typing, configuration keys, default
  parameters, the R1200B `full_template` selection in `config_specific_par`,
  and `get_rawimage`. Three new class attributes are declared for children to
  set: `detector_name` (the `DETECTOR` header value), `detector_platescale`,
  and `detector_specflip`; `get_detector_par` now uses these instead of an
  internal plate-scale dict, and warns if the header `DETECTOR` disagrees
  with the class.
- New `check_spectrograph` on the parent (called automatically by
  `PypeItMetaData`): raises a `PypeItError` naming the sibling class if the
  data were taken with the other camera.
- `INTIDSEEV10Spectrograph` → `name='int_ids_eev10'`, camera EEV10,
  0.40"/pix, `specflip=True` (all verified against the test data).
- `INTIDSREDPLUS2Spectrograph` → `name='int_ids_redplus2'`, camera RED+2,
  0.44"/pix. Explicitly documented as a placeholder: the exact `DETECTOR`
  string (assumed 'REDPLUS2'), amp layout, gain/read-noise cards, and
  spectral flip all still need verification against real RED+2 frames.

**Validation (pypeit14b):**

- `load_spectrograph('int_ids')` correctly fails; both child names load.
- `int_ids_eev10`: frame typing of the four test frames unchanged
  (bias / pixelflat,illumflat,trace / arc,tilt / science), detector par
  correct (0.40"/pix, specflip=True, gain 1.2), and `config_specific_par`
  still selects `full_template` + `int_ids_R1200B.fits` for R1200B.
- `int_ids_redplus2` loads with its placeholder values and its
  `check_spectrograph` correctly rejects the EEV10 test data, pointing the
  user at `int_ids_eev10`.
- PypeIt unit tests `test_spectrographs.py` and `test_pypeitpar.py`:
  45 passed.

Note: task 4 (`pypeit_setup`) should now use `-s int_ids_eev10`.

### 2026-08-24 (Acted on Q&A #1: wavelength template, specflip, RED+2 placeholder)

Executed prompt 2, acting on the first round of Q&A answers. (Model note: the
session again ran on Claude Fable 5; the main session model cannot be switched
to Opus from within the session.)

**IRAF solution (`pypeitdev/int_ids/idarc_bs.ms`, Q&A #5):**

- It is an IRAF `identify` database record with 11 identified lines
  (CuI 5218/5105, ArII 4880–4348, ArI 4201/4104/3946) over pixels 404–3066 of
  the 4200-row spectral axis, plus a spline3 dispersion function.
- Wavelength *decreases* with row number (~0.478 A/pix), settling Q&A #3:
  set `specflip=True` in `int_ids.py` for EEV10.
- Verified the solution applies to our `arc.fits`: all strong lines fall
  within ±0.5 pix of the IRAF centroids (the faint 3946 A line is off by
  ~6 pix, but it carries little weight).
- Refit the 11 (pixel, wavelength) pairs with a 4th-order Chebyshev:
  RMS = 0.22 A. Coverage of the full frame: ~3422–5406 A (header CENWAVE says
  4502; true center is ~4434).

**Template built:**

- New script `pypeitdev/int_ids/build_R1200B_template.py` extracts a boxcar
  arc spectrum at the IRAF aperture (spatial pixels 168–178), subtracts the
  overscan level, attaches the Chebyshev wavelengths, flips to the PypeIt
  convention (wavelength increasing), and writes
  `pypeit/data/arc_lines/reid_arxiv/int_ids_R1200B.fits` via
  `wvutils.write_template`.

**PypeIt code changes (branch `int_ids`):**

- `specflip=True` (with a comment citing the IRAF verification).
- New `config_specific_par`: grating `R1200B` uses
  `method='full_template'` with `reid_arxiv='int_ids_R1200B.fits'`; other
  gratings fall back on the `holy-grail` default.
- RED+2 placeholder per Q&A #4: a `plate_scales` dict (`EEV10: 0.40`,
  `REDPLUS2: 0.44`) with a warning (and EEV10 fallback) for unrecognized
  `DETECTOR` strings; comments note that RED+2's exact DETECTOR string,
  amp layout, gain, and read noise remain unverified.

**Validation (pypeit14b, per Q&A #7):**

- Ran `autoid.full_template` end-to-end on an arc spectrum extracted at a
  *different* spatial position (columns 200–230) than the template:
  cross-correlation cc = 0.96, **77 lines identified** from the
  NeI/ArI/ArII/CuI lists, final fit **RMS = 0.127 pixels (~0.06 A)**,
  monotonically increasing solution over 3461–5410 A. The template, the
  lamp list, and `specflip=True` all check out.

**Prompt-file fixes (Q&A #6/#7):** report paths in tasks 3 and 5 changed from
`pypeitdev/hane_hamspec/Reports` to `pypeitdev/int_ids/Reports`, and task 5
now says the `pypeit14b` environment.

No new blocking questions. Ready for prompt 3 (`pypeit_setup` + report).

### 2026-08-24 (Created the int_ids spectrograph module)

Executed Prep task 1. Note: the prompt requested Opus; this session ran on
Claude Fable 5 (the session's configured model).

**Context examined:**

- The ING/IDS web pages: long-slit spectrograph at the INT (2.5 m) Cassegrain
  focus; two 4k×2k CCDs — EEV10 (blue, 0.40"/pix) and RED+2 (red, default,
  0.44"/pix); 16 gratings (0.24–4 Å/pix); 3.3' unvignetted slit; 235 mm camera.
  IDS was withdrawn from operation in June 2024 (HARPS3 installation).
- The four raw frames in `pypeitdev/int_ids/INT_20201218/` (arc, bias, flat,
  science; there is also a `coffee.png`). All are EEV10, grating R1200B,
  cenwave 4502 Å, 1×1 binning, single LH amplifier, windowed readout: data in
  extension 1, shape 366(spatial)×4200(spectral), `TRIMSEC=[1:366,1:4099]`,
  overscan `BIASSEC=[1:366,4105:4190]`, gain 1.2 e-/ADU, RN 4.2 e- (SLOW).
  Frame typing is clean via `IMAGETYP` (object/zero/flat/arc) and `AGARCLMP`
  (arc lamps, e.g. 'CuAr CuAr CuNe CuN'; 'W' = tungsten flat lamp; blank
  otherwise). `MJD-OBS` is provided directly; RA/DEC are sexagesimal strings.

**Code written (PypeIt repo, new branch `int_ids` off `develop`):**

- `pypeit/spectrographs/int_ids.py` — new `INTIDSSpectrograph` class modeled
  on `mmt_bluechannel.py`. Notable choices:
  - `get_detector_par` reads gain/RN/`TRIMSEC`/`BIASSEC` from the ext-1 header
    and flips the FITS (x,y) section strings into numpy (spec,spat) order via a
    small `_fits_to_pypeit_section` helper; platescale switches on `DETECTOR`
    (EEV10 = 0.40, RED+2 = 0.44); `dataext=1`, `specaxis=0`.
  - `get_rawimage` just calls the base class with `sec_includes_binning=True`
    since the header sections are in read-out (windowed/binned) coordinates.
  - `compound_meta` converts sexagesimal RA (hours) / DEC (deg) to decimal
    degrees, parses space-separated `CCDSUM` (x-bin first, so spatial,spec),
    and maps blank `AGARCLMP` to 'off'.
  - Configuration keys: `dispname` (GRATNAME), `cenwave` (CENWAVE),
    `detector` (DETECTOR), `binning`.
  - Frame typing by `idname` (IMAGETYP); science/standard split by exposure
    time (see Q&A #2).
  - Wavelengths: `holy-grail` with NeI/ArI/ArII/CuI (no template yet);
    long-slit edge params (`sync_predict='nearest'`, `bound_detector=True`).
- `pypeit/spectrographs/__init__.py` — registered `int_ids` (alphabetical).
- `pypeit/telescopes.py` — added `INTTelescopePar` (Roque de los Muchachos
  site, 2.5 m), modeled on the WHT entry.
- `pypeit/par/pypeitpar.py` — added 'INT' to `valid_telescopes`.

**Verification (pypeit14b env, editable install of this checkout):**

- `load_spectrograph('int_ids')` works.
- Metadata extraction on all four frames returns correct ra/dec (decimal
  deg), mjd, exptime, dispname, cenwave, lamps, etc.
- `get_rawimage` returns a (4200, 366) image with datasec rows 0–4098 and
  overscan rows 4104–4189, correctly oriented.
- `PypeItMetaData.get_frame_types` types the frames as bias /
  pixelflat,illumflat,trace / arc,tilt / science, all in one configuration.

**Left for later**: `specflip` verification against the arc, RED+2 detector
parameters, a wavelength template for R1200B, dev-suite registration
(`.pypeit` file, `test_setups.py`, `test_load_images.py`), and docs
(release notes, `doc/spectrographs/`). Questions are in Q&A above.