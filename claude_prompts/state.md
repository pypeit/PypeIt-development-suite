# PypeIt State

## Goals

This doc guides development of the PypeIt RunPypeItState class and related code.

## Claude

### Skills

Consider using the skills in PypeIt-development-suite/.claude/skills/

## Coding

Here are guidelines for coding:

- Use Python where possible
- Add inline comments to explain the effort
- Reuse existing code when possible
- Place import statements at the top of the file.
- Include a description of inputs/outputs in the doc string of all methods
- Limit lines of code to 80 characters per line
- Add "Generated by JXP and Claude" in the doc string of all methods you create

If you need to run Python, use the "pypeit14" environment in conda.

## Context

Read the following documents to gain context:

- PypeIt-development-suite/pypeitdev/dashboard/pypeit_workflow.md
- PypeIt-development-suite/pypeitdev/dashboard/pypeit_dashboard_design.md
- PypeIt-development-suite/pypeitdev/dashboard/pypeit_dashboard_coding.md
- The state source: PypeIt/pypeit/state.py (`RunPypeItState`, `load()`,
  `get_status()`, the `(calib_id, det)` / step structure) and
  PypeIt/pypeit/scripts/pypeit_status.py (the no-processing derive path)

## Development

### Safety checks

I am worried that the new state class, and especially its I/O will crash PypeIt runs.  Before I proceed with a pull request, I wish to limit the likelihood of this happening.

#### Tasks

1. Please:

   - Inspect the pypeitdev/dashboard/pypeit_workflow.md document and identify any tasks that could cause a crash.
   - Inspect the code base starting with pypeit.py and identify any tasks that could cause a crash.
   - Report your findings in the Logs section below.
   - Include any recommendations 
   - If you have any questions, put them under the Q&A section below and I will answer them.

2. I answered the questions under Q&A.  Please:

   - Fix these findings in the Logs section:
     - C1: write `'fail'`, not `'failed'`.
     - C2: replace the `except: embed` blocks with `log.warning(...)` + continue
     - C3: modify the code to allow for the mosaic `det` to be a tuple
     - C5: Add a safe write method that logs and continues and a safe update method 
   - Log your work


3. Now, please:

   - Perform a PypeIt run in the $PYPEIT_DEV/REDUX_OUT/shane_kast_blue/830_3460_d46
     - Monitor the generation of the state JSON file
     - Identify any potential issues
   - Generate new unit tests to add for the new state class and its I/O.  Put these in a PypeIt/pypeit/tests/test_state.py file.
   - Log your work

#### Q&A

1. **Is `RunPypeItState.load()` meant to be enabled before the PR?** It is
   currently commented out at `pypeit.py:74` and `pypeit_status.py:64`. This
   drives the severity of finding **C4**: with `load()` off, a corrupt/old state
   file is harmless to a run; with it on, it crashes `PypeIt.__init__` for every
   run in that directory until the file is deleted.

For the time being, I will not use the `load()` method during a PypeIt run.  However, the dashboard will use it.  Frequently

2. **When `load()` (or the dashboard) hits a malformed / schema-outdated
   `*_state.json`, should it silently rebuild from scratch, or fail loudly?**
   This decides whether `load()` should swallow `ValidationError` and start
   fresh (my recommendation) or surface it.

For the dashboard, it should rebuild from scratch with a warning.  But you don't need to worry about this for the time being.

## Prompts

1. Read this doc.  Perform the 1st task under Safety checks.
2. Read this doc.  Perform the 2nd task under Safety checks.

## Logging

The "Logs" section will record Claude's work.  Please use the following format:

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

...

## Logs

### 2026-06-13 (Safety audit of RunPypeItState and its I/O — Task 1)

Performed Task 1 under *Safety checks*: reviewed `pypeit_workflow.md` and the
code path starting at `pypeit.py`, looking specifically for ways the new
`RunPypeItState` class and its I/O could crash a `run_pypeit` reduction. Each
of the two highest-severity findings was **reproduced** with a short script
against the installed code (no RAW_DATA needed); the rest are from reading.

**Where state is wired in (the surface that can affect a run):**
- `pypeit.py:70` constructs `run_state` in `PypeIt.__init__`; `:127`
  `required_calibs(...)` populates the `required` flags; the object is threaded
  into `calib_all` / `reduce_all` → `pypeit_steps.calib_one` → `Calibrations`
  (`state=run_state`, `calibrations.py:203`).
- `calibrations.py:run_the_steps` (`:1489`) is the hot loop: it sets
  `status='running'`, runs the step, sets success/fail, and calls
  `state.write()` on every step transition (`:1499, :1514, :1528`). This is the
  I/O that lands in the reduction's inner loop, so it is the main crash surface.
- `load()` is currently **commented out** at `pypeit.py:74` and
  `pypeit_status.py:64`, so today a run does not *read* a prior state file.

**Findings (severity in caps):**

- **C1 — HIGH, reproduced. Invalid status string `'failed'` poisons the state
  file.** `calibrations.py:1520` writes `status='failed'`, but the
  `BaseCalibState.status` Literal only allows
  `complete|fail|undone|running|success`. pydantic v2 does **not** validate on
  assignment, so the run keeps going and writes the file — but
  `RunPypeItState.load()` / `model_validate` then raises `ValidationError` on
  that file (reproduced: `Input should be 'complete', 'fail', ... input_value
  ='failed'`). Impact: any reduction with a failed calibration step produces a
  `*_state.json` that **cannot be reloaded** → the dashboard's load path (R4)
  crashes, and `run_pypeit` itself would crash on startup the moment `load()`
  (pypeit.py:74) is uncommented. **Fix: write `'fail'`, not `'failed'`.**

- **C2 — HIGH. Bare `except: embed(...)` around `state.write()`.**
  `calibrations.py:1527-1530` wraps the in-loop write in
  `try: ... except: embed(header='1551 of calibrations')`. If a write ever
  raises, the reduction drops into an **IPython embed shell mid-run**. In
  non-interactive contexts (dev suite, Nautilus/cron, `nohup`) this hangs or
  errors out the run; interactively it silently blocks the pipeline. It also
  swallows the real exception. **Fix: replace with `log.warning(...)` + continue
  — state I/O must never abort a reduction.** (A second commented `embed` sits at
  `:1367`; remove it too.)

- **C3 — MEDIUM-HIGH, reproduced. Mosaic detectors break `update_calib`
  matching → duplicated / corrupt state.** For mosaics, `det` is a tuple (e.g.
  `(1, 5)` for DEIMOS/GMOS/MOSFIRE), passed straight from
  `spectrograph.select_detectors()` (confirmed: that method returns tuples for
  mosaics). pydantic coerces the stored `det: int | List[int]` field to a list
  `[1, 5]`, so the lookup `item.calib_id == calib_id and item.det == det`
  compares `[1,5] == (1,5)` → always False. Every `update_calib` call therefore
  **creates a new entry** instead of updating the existing one. Reproduced: 3
  separate `bias` entries after 3 calls, with `running`, `success`, and the
  `mean` metric scattered across different entries. The run does not crash
  (`update_calib` never raises), but the state is bloated and wrong, and the
  dashboard view is meaningless for every mosaic instrument. The TODO at
  `state.py:154` already flags this. **Fix: normalize `det` to one canonical type
  (tuple→list) on both the stored value and the comparison key.**

- **C4 — HIGH (latent). `load()` has no schema/version guard.** When `load()` is
  re-enabled (clearly intended — see commented `pypeit.py:74`,
  `pypeit_status.py:64`), any pre-existing `*_state.json` that is partial (C1),
  hand-edited, or written by an older schema will raise `ValidationError` and
  crash `PypeIt.__init__` for **every** subsequent run in that directory until
  the file is manually deleted. PypeIt `DataContainer`s guard this with
  `chk_version`; the pydantic state has no equivalent. Also `load(path=...)`
  ignores its `path` argument (it uses `self.outfile`). **Fix: wrap `load()` in
  try/except that logs and falls back to a fresh state; add a schema-version
  field and check it.**

- **C5 — LOW-MEDIUM. The other state mutations/writes are unguarded.** The first
  two `state.write()` calls in `run_the_steps` (`:1499, :1514`) and all the
  `update_calib` calls are not protected. I verified that numpy metric values
  (`image.mean()` → `np.float64`, `np.int64` dets) currently **do** serialize
  fine, so these don't crash today — but any future state op that raises would
  abort the reduction. Given the stated goal (state I/O must never crash a run),
  a single guarded `_safe_write()` / `_safe_update()` wrapper used everywhere is
  the robust fix.

- **C6 — LOW. `required_calibs()` is now on the critical path of every run**
  (`pypeit.py:127`, unconditional, even for `calib_only`/status). It iterates
  `fitstbl.find_frames(...)`; it looks safe, but it is new code newly in
  `__init__`, so an exception there aborts startup. Worth a guard and/or
  dev-suite coverage across all three pipelines.

**Workflow-doc review:** `pypeit_workflow.md` is descriptive, not executable;
the only crash-relevant "tasks" it introduces are the state-file writes injected
into the §3.3 calibration loop and the §6.2 `*_state.json` / `*.status.log`
artifacts — i.e. exactly the `run_the_steps` write path covered by C1/C2/C5.

**Recommendations (priority order):**
1. `'failed'` → `'fail'` (C1) and delete the `except: embed` blocks, replacing
   with logged warnings (C2). These two are the concrete run/IO crashers.
2. Fix mosaic `det` matching (C3) — otherwise state is wrong for a large class
   of supported instruments.
3. Before enabling `load()`, make it crash-proof: try/except + fresh-state
   fallback + a schema version field (C4).
4. Adopt the principle that **all** state collection and I/O is wrapped so it can
   only ever log-and-continue, never abort a reduction (C5/C6).
5. Tests: add CI-safe unit tests in `pypeit/tests/` that round-trip (a) a state
   with a failed top-level step and (b) a mosaic `det` — both reproduce the bugs
   deterministically without RAW_DATA. Then exercise a mosaic spectrograph (e.g.
   `keck_deimos`) and a deliberately-failing calibration setup in the dev suite.

Two questions for the user are recorded under *Q&A* above (whether `load()` is
to be enabled for the PR, and how a malformed state file should be handled).

### 2026-06-13 (Fixed safety findings C1, C2, C3, C5 — Task 2)

Implemented the four fixes requested under Task 2. All changes were verified
with a short repro script in the `pypeit14` env (the same scenarios that
exposed the bugs in Task 1 now pass), and `pypeit.state`, `pypeit.calibrations`,
and `pypeit.pypeit` all still import cleanly.

**C1 — invalid `'failed'` status string (`pypeit/calibrations.py`).**
In `run_the_steps`, the failure branch now writes `status='fail'` (a valid
`BaseCalibState.status` Literal) instead of `'failed'`. A state file produced by
a failed calibration step now reloads via `RunPypeItState.load()` instead of
raising `ValidationError`. Verified: write a `fail` status → `load()` returns it.

**C2 — `except: embed(...)` blocks (`pypeit/calibrations.py`).**
- Replaced the bare `try: self.state.write() / except: embed(...)` in
  `run_the_steps` with the new `safe_write()` wrapper (logs a warning and
  continues). A state-write failure can no longer drop a reduction into an
  IPython shell or abort it.
- Removed the dead `#embed(header='1288 of calibrations')` comment in the tilts
  per-slit state method. (The module-level `from IPython import embed` import is
  left in place, matching PypeIt's house convention; it is no longer called.)

**C3 — mosaic `det` tuple mismatch (`pypeit/state.py`).**
Added a module-level helper `same_det(det1, det2)` that normalizes a mosaic
(list/tuple) to a tuple before comparing, and leaves scalar (single-detector)
ids as-is. `update_calib()` and `get_status()` now use `same_det(...)` for the
`det` comparison instead of a raw `==`. A mosaic such as `(1, 5)` (stored by
pydantic as `[1, 5]`) now matches on every call, so repeated `update_calib`
calls update a **single** entry rather than spawning duplicates. Verified:
3 updates to det `(1, 5)` now yield 1 entry carrying status `success` and
`mean=1.0` together (previously 3 split entries).

**C5 — unguarded state mutations / writes (`pypeit/state.py`,
`pypeit/calibrations.py`).**
- Added two wrappers on `RunPypeItState` so state bookkeeping can never crash a
  reduction:
  - `safe_write()` — calls `write()` inside `try/except`, logging a warning and
    returning `False` on failure (and the existing `write()` gained a docstring
    pointing callers at it).
  - `safe_update_calib(...)` — same protection around `update_calib(...)`.
- `run_the_steps` now uses `safe_write()` / `safe_update_calib()` for all three
  in-loop state touchpoints (initial write, `running` marker, end-of-step write).
- Additionally wrapped the per-step metric collector dispatch
  (`getattr(self, f'{step}_state')()`) in `try/except` + `log.warning`, since
  that code reads numpy arrays / slit bitmasks and was on the otherwise-unguarded
  success path. Verified: `safe_write()` to an unwritable path logs and returns
  `False` without raising.

**Coding notes.** All new methods/functions carry "Generated by JXP and Claude"
in their docstrings, lines kept ≤ 80 chars, imports at the top
(`from pypeit import log` added to `state.py`; confirmed non-circular since
`pypeit/__init__.py` defines `log` before any submodule import).

**Not done (out of scope for Task 2, per Q&A):** C4 (`load()` schema/version
guard + fresh-state fallback). Per the user's Q&A answers, `load()` is not used
during a run for now, and the dashboard's rebuild-on-malformed behavior is
deferred — so C4 is intentionally left for later. Task 3 (a real
`shane_kast_blue/830_3460_d46` run + `test_state.py` unit tests) is also not
started.
