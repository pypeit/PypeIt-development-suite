# Develop Stage 6 of the PypeIt Dashboard

## Goals

Continuing the staged build defined in `pypeit_dashboard_coding.md`
(*Developing → Build order*), this document guides **Stage 6 — Science frames**:
populate the **Science** section (a placeholder since Stage 2) so the Dashboard
shows, per reduced science/standard exposure, how far its reduction got and how
good it looks — the design's R15/R18.

The science **state layer is already done** (see
`claude_prompts/state_science.md`): `RunPypeItState` records a `science` list of
`ScienceFrameState` entries (one per `(frame, det)`) with the four macro-step
statuses (`process` → `findobj` → `skysub` → `extract`), the `spec2d`/`spec1d`
products, `nobj`, and per-slit / per-object detail; it is populated **live**
during a run and **derived from disk** when there is no state file. Stage 6 is
the **Qt view** over that data — the last build-order stage.

Stage 6 builds on what already exists:

- **Stage 1** — `DashboardModel` (loads/derives the state; the calib accessors
  `status_table`/`slit_table`/`step_metrics`; `is_stale`, `log_path`,
  `state_path`).
- **Stage 2** — the `StatusView` with its **placeholder Science section** and the
  status palette + glyph encoding to reuse.
- **Stage 3** — the `CalibrationsView` drill-down pattern (scope selectors,
  detail panel, per-slit table) and the `Launcher` (subprocess inspection).
- **Stage 5** — live monitoring (`RunLock.stateChanged` →
  `MainWindow._refresh_from_state`) and the two-channel `ActivityBar`; a
  model-driven Science view auto-refreshes for free.

The implemented state API the view consumes (in the **`pypeit/state/`** package):
`RunPypeItState.get_science_status()` → a per-frame DataFrame with columns
**`frame`, `detector`, `objtype`, `process`, `findobj`, `skysub`, `extract`,
`nobj`, `spec2d`, `spec1d`**; per-frame `ScienceFrameState.slits`
(`ScienceSlit`: status from `BADSKYSUB`/`BADEXTRACT`, `nobj`) and `.objects`
(`ScienceObj`: `snr_find`, `s2n`, `spat_pixpos`, `fwhm`, `sign`, `extracted`);
and `pypeit.state.science_status.derive_science_from_disk` for the no-state-file
case.

**Scope note (updated after the S6-Q answers).** Stage 6 **presents** science
state, launches the **existing** product viewers (per-object for 1D, S6-Q3), and
— per **S6-Q6** — adds a **(Re)Build** control mirroring the Calibrations one,
launching `pypeit_reduce_by_step` to (re)run a science step (`process` /
`findobj` / `extract`) for a frame/detector. (This is the science analogue of
the Stage 4 calib (Re)Build; the products are the `Intermediate/` files +
`spec2d`/`spec1d`.) Launching a **full `run_pypeit`** stays out of scope (it is
only *observed*); the `.log` tail view stays deferred; deferred science metrics
(`OPT_CHI2`, `WAVE_RMS`, flexure — `state_science.md` Q6) are not surfaced until
the state model carries them. *The science (Re)Build has open design points —
see S6-Q10–S6-Q12.*

Each stage below carries the design requirements it satisfies (the `R` items in
`pypeit_dashboard_design.md`), a concrete task breakdown, the files it touches,
and acceptance criteria. Open implementation choices are raised in the
**Clarifications** channel for the user to answer inline before coding begins.

## Claude

### Skills

Consider using the skills in PypeIt-development-suite/.claude/skills/
Especially:

- pyqt-expert
- pyqt6-ui-development-rules

## Coding

Here are guidelines for coding:

- Use Python where possible
- Add inline comments to explain the effort
- Reuse existing code when possible
- Place import statements at the top of the file.
- Include a description of inputs/outputs in the doc string of all methods
- Limit lines of code to 80 characters per line
- Add "Generated by JXP and Claude" in the doc string of all methods you create
- Production Dashboard code goes in `PypeIt/pypeit/dashboard/`
- Design-phase scratch/harness code (e.g. the layout-check harness) goes in
  `PypeIt-development-suite/pypeitdev/dashboard/py/`
- Follow the GUI conventions in `pypeit_dashboard_coding.md` (qtpy-only imports;
  `Signal`/`Slot`, not `pyqtSignal`/`pyqtSlot`)

## Context

Read the following documents to gain context:

- PypeIt-development-suite/pypeitdev/dashboard/pypeit_dashboard_design.md
  (especially the **Science-frame status** note and **R15/R18** — the Science
  view spec; the status palette R10; the summary R7; grouping R8)
- PypeIt-development-suite/pypeitdev/dashboard/pypeit_dashboard_coding.md
  (*Developing → Build order* — Stage 6 row; the MVC split; the test strategy)
- PypeIt-development-suite/claude_prompts/state_science.md
  (the science-state plan, Q&A, and Logs — what each step/metric means and where
  it comes from; the derive-from-disk priority)
- PypeIt-development-suite/pypeitdev/dashboard/pypeit_workflow.md
  (the science reduction products — `spec2d`/`spec1d` — and the `obj_prof` /
  `obj_trace` QA the per-object metrics correspond to)
- PypeIt-development-suite/claude_prompts/dashboard/dashboard_dev_stage_3.md,
  dashboard_dev_stage_4.md, dashboard_dev_stage_5.md (the Calibrations view
  drill-down pattern, the `Launcher`, the `RunLock`/live-refresh, the
  two-channel `ActivityBar`)
- The built dashboard code: PypeIt/pypeit/dashboard/ — especially
  `model.py` (where the **science accessors** go, mirroring the calib ones),
  `inspect.py` (where the spec2d/spec1d command builders go),
  `view/status_view.py` (the placeholder Science section), `view/calibrations_view.py`
  (the drill-down + detail-panel pattern to mirror), `view/main_window.py`
  (tabs + the refresh wiring), `palette.py` (status palette + `slit_style`)
- The state layer: PypeIt/pypeit/state/run_state.py
  (`ScienceFrameState`/`ScienceObj`/`ScienceSlit`/`ScienceStep`, `science_steps`,
  `get_science_status`, `science_entry`) and PypeIt/pypeit/state/science_status.py
  (`derive_science_from_disk`)
- The product viewers: PypeIt/pypeit/scripts/show_2dspec.py
  (`pypeit_show_2dspec <spec2d>` [+ `--det`]) and show_1dspec.py
  (`pypeit_show_1dspec <spec1d>`)
- **Test datasets**: `shane_kast_blue_A` 600/4310 in `$PYPEIT_DEV/REDUX_OUT`
  (3 exposures — b24 standard, b27, b28 — already reduced with science state, per
  `state_science.md`); a CI-safe synthesized science `*_state.json` fixture for
  the `pytest-qt` tests (mirroring the calib fixtures).

## Development

This section defines Stage 6. Stage references (`R#`) point at
`pypeit_dashboard_design.md`; the stage table is in `pypeit_dashboard_coding.md`
(*Developing → Build order*).

### Stage 6 — Science frames

**Design refs:** R15 (the Science view — per-frame rows + per-slit/per-object
drill-down, reusing the palette R10 and the summary R7), R18 (an
equally-prominent, filterable/scrollable Science section), R8 (grouping). Builds
on the implemented `RunPypeItState.science` state layer.

**Goal.** Replace the Science placeholder with a real view that renders
`get_science_status()` — one row per `(frame, det)` with the four macro-step
statuses (color+glyph), `nobj`, and `spec2d`/`spec1d` presence, standards tagged
in the same table — plus a per-frame drill-down (per-slit + per-object tables)
and product-viewer launches, all reusing the Stage 3 patterns and the Stage 5
live-refresh.

**Mechanism** (S6-Q answers folded in; open science-(Re)Build points in
S6-Q10–S6-Q12):

1. **A third Science tab** (S6-Q1) — Status | Calibrations | **Science** — with a
   per-frame **table** + drill-down mirroring the Calibrations view; plus a
   **compact Science summary** in the Status view (counts only) and the summary
   strip extended to span science (S6-Q9).
2. **Model accessors** (Qt-free, mirroring the calib ones) — add to
   `DashboardModel`: `science_table()` (normalized `get_science_status`),
   `science_frame_entry(frame, det)`, `science_slit_table(frame, det)`,
   `science_object_table(frame, det)`; and **derive science on launch** when
   there is no state file via `derive_science_from_disk(run_state, redux_dir,
   fitstbl)` (S6-Q7).
3. **The per-frame table** — **flat rows** per `(frame, det)` (S6-Q4) with an
   explicit **`objtype` column** (science/standard, S6-Q5); the four macro-step
   statuses rendered as **palette color+glyph cells** (S6-Q6); `nobj` and
   `spec2d`/`spec1d` presence; filterable/scrollable (R18).
4. **Per-frame detail panel** (S6-Q2, mirrors Calibrations) — a per-slit
   `ScienceSlit` table (status from `BADSKYSUB`/`BADEXTRACT`, `nobj`) and a
   per-object `ScienceObj` table (snr_find, s2n, spat_pixpos, fwhm, sign,
   extracted); product/per-object viewers and the (Re)Build control below.
5. **Inspection viewers** — `inspect.py` gains `spec2d_command`
   (`pypeit_show_2dspec <spec2d> [--det]`) and a **per-object** `spec1d_command`
   that launches `pypeit_show_1dspec <spec1d> --obj <name>` for the selected
   object, **reconstructing the object name** (S6-Q10 option (a))
   `SPAT{round(spat_pixpos):04d}-SLIT{slitid:04d}-{detname}` from the
   `ScienceObj` (`spat_pixpos`, `slitid`) + the frame's `det` — matching
   `SpecObj.set_name` (e.g. `SPAT0424-SLIT0000-DET01`). Gated on the product
   existing; reported on the **Inspection** ActivityBar channel. *(Impl caveat:
   PypeIt names objects from `SPAT_PIXPOS_ID`; if a reconstructed name ever
   misses, fall back to `--list`.)*
6. **(Re)Build control** (S6-Q6, the science analogue of Stage 4) — per
   `(frame, det)` step, a blue (Re)Build that launches
   `pypeit_reduce_by_step <pf> <frame> <step> --det` (`step` ∈
   process/findobj/extract), governed by the **same `RunLock`** (single-run
   lock + live monitoring). Resolved: **(S6-Q11)** mirror the `run_to_calibstep`
   state-write fix — thread `run_state` through `reduce_by_step` (via the science
   recorders) + a final `derive_science_from_disk` write — so the science
   (Re)Build is observable/live; **(S6-Q12)** clobber = **re-run with a
   confirmation** naming the file(s) `reduce_by_step` will overwrite (no
   move-aside), and **enable a step only when its prerequisite product exists**
   (process always available; `findobj` needs the `process` product; `extract`
   needs the `findobj` product).
7. **Live refresh** — the view is model-driven, so Stage 5's
   `_refresh_from_state` re-renders it during a run with no extra wiring.

**Deliverables / tasks.** *(all S6-Q resolved)*

1. Model science accessors + science derive on launch (S6-Q7).
2. `inspect.py`: `spec2d_command`, per-object `spec1d_command` (reconstructed
   `--obj`, S6-Q10), and `science_run_command` (`pypeit_reduce_by_step`).
3. The `reduce_by_step` **state-write fix** (S6-Q11) — thread `run_state` through
   it (science recorders) + a final `derive_science_from_disk` write, mirroring
   the Stage-4 `run_to_calibstep` fix.
4. The Science tab (table + detail panel) + the Status-view compact summary,
   wired into the window + the live refresh.
5. The science **(Re)Build** control (S6-Q12): re-run with a file-naming
   confirmation, reuse the `RunLock`, enable a step only when its prerequisite
   product exists.
6. Summary-strip extension (R7, S6-Q9).
7. CI-safe `pytest-qt` tests over a synthesized science fixture + a science model
   fixture; harness renders + milestone screenshots. (The real
   `pypeit_reduce_by_step` (Re)Build is an interactive Sign off.)

**Acceptance criteria.** *(all S6-Q resolved)*

- The Science tab shows flat `(frame, det)` rows with an `objtype` column, the
  four step statuses (palette color+glyph), `nobj`, and `spec2d`/`spec1d`;
  scrollable; standards tagged.
- Selecting a frame shows its per-slit and per-object detail; "View spec2d"
  launches `pypeit_show_2dspec`, and a per-object "View 1D" launches
  `pypeit_show_1dspec` for the **selected object**.
- A (Re)Build relaunches the chosen science step (`pypeit_reduce_by_step`),
  governed by the run-lock, and the view updates (live + on completion).
- A launch with no state file still shows science derived from disk (S6-Q7).
- 0 objects reads as success; CI-safe tests pass offscreen; light/dark legible.

#### Sign offs

Actions for **you** to sign off on Stage 6. As before, the key human checks are
the **interactive** ones — the Science tab on a real reduction and a real science
(Re)Build. Run from the PypeIt repo root (`/home/xavier/Projects/PypeIt/PypeIt`).

1. **Automated tests pass (CI-safe).**
   - Execute: `python -m pytest pypeit/tests/test_dashboard.py -v`
   - Inspect: **all passed** — incl. the new Stage 6 tests:
     `test_model_science_accessors` (the science table + per-slit/per-object
     detail), `test_inspect_science_commands` (the `pypeit_show_2dspec` /
     per-object `pypeit_show_1dspec --obj` / `pypeit_reduce_by_step` argv +
     the reconstructed object name), `test_science_view_table_and_detail`,
     `test_science_view_rebuild_buttons` (prerequisite + lock gating), and
     `test_main_window_has_science_tab` (the third tab). (`+ test_state.py`
     covers the science state model.)

All 55 tests pass

2. **Review the Science screenshot.**
   - Inspect ``$PYPEIT_DEV/pypeitdev/dashboard/py/layout_check/dashboard_science_view.png``.
   - Confirm: a **third Science tab**; the per-frame table with an **objtype**
     column (science/standard), the four step-status cells, `nobj`, and
     `spec2d`/`spec1d`; and the detail panel with the per-slit table, the
     per-object table (`snr_find`, `s2n`, …), **View spec2d**, and the three blue
     **(Re)Build** buttons.

Looks good

3. **Launch the GUI on a real reduction — the interactive check.**
   - Execute (a reduction with science products, e.g. the dev-suite
     `shane_kast_blue` 600/4310): `pypeit_dashboard $PF`
   - On the **Science** tab: confirm the science **and standard** frames are
     listed with their step statuses; select a frame and confirm its per-slit
     and per-object tables; **double-click an object** and confirm its **1D**
     spectrum opens (`pypeit_show_1dspec` on that object); click **View spec2d**
     and confirm the 2D spectrum opens (`pypeit_show_2dspec`). The **Inspection**
     status channel reports each launch.

4. **Science (Re)Build — the interactive run.**
   - On a science frame, click **(Re)Build process** (or `findobj` / `extract`);
     confirm the dialog naming the step, accept, and watch: the **(Re)Build**
     controls turn orange "⏳ Run in progress" (the run-lock), the run executes
     `pypeit_reduce_by_step`, and on completion the Science view **updates** (the
     step's status / products refresh). Confirm a step is **disabled until its
     prerequisite** has succeeded (e.g. `extract` needs `findobj`).

5. **Derive on launch + live monitoring.**
   - Launch the dashboard on a reduction **with no** `*_state.json`: confirm the
     Science tab still populates (derived from the on-disk `spec2d`/`spec1d`).
   - Start `run_pypeit` (or a `pypeit_reduce_by_step`) in a terminal and confirm
     the Science view **auto-updates** as the reduction progresses.

**Sign-off:** if items 1–3 look right, Stage 6 is accepted and the dashboard
build order (Stages 0–6) is complete. Note anything off (item + screenshot) for a
Modifications round.

#### Clarifications

*(Discussion channel for Stage 6. Recommendations and open questions below —
please answer inline under each **S6-Q**. No code until these are resolved.)*

- **S6-Q1 — Where the Science view lives.** A dedicated **third tab**
  (Status | Calibrations | **Science**) with a per-frame table + drill-down
  (mirroring Calibrations), or populate the **Status view's** existing Science
  section in place? R18 wants it "equally prominent" alongside calibrations.
  I lean a **third tab** for the rich drill-down, and keep a **compact
  Science summary** in the Status view (counts only) so the state-first view
  still surfaces it. Agree, or do you want it entirely within the Status view?

Yes, a third tab is the right place for the Science view.

- **S6-Q2 — Per-frame detail pattern.** Mirror the Calibrations detail panel:
  select a frame → a panel with the per-slit table (`ScienceSlit`: status,
  nobj) and the per-object table (`ScienceObj`: snr_find, s2n, spat_pixpos,
  fwhm, sign, extracted), plus the product viewers. Agree this is the right
  reuse?

Yes, that is right.

- **S6-Q3 — Product viewers.** Launch `pypeit_show_2dspec <spec2d>` (with
  `--det`) and `pypeit_show_1dspec <spec1d>`, gated on the product existing,
  reported on the **Inspection** ActivityBar channel (Stage 5). These open Ginga
  / a matplotlib window respectively — confirm, and confirm we do **not** also
  need a per-object 1d launch (vs the whole spec1d).

That is correct for the 2D images.  For the 1D spectra, one does need to select the object to view.  Be sure to enable this.

- **S6-Q4 — Row granularity & grouping (R8).** `get_science_status()` is one row
  per `(frame, det)`. For multi-detector runs, group rows by **frame** (with a
  detector sub-column / sub-rows), or list flat `(frame, det)` rows? I lean
  **flat rows** for v1 (simplest, matches the DataFrame), with a detector column;
  grouping by frame can come later if needed.

Ok, go flat for v1.

- **S6-Q5 — Standards display.** Standards share the table tagged by `objtype`
  (decided in `state_science.md` Q11). Show the tag as an **`objtype` column**
  (science/standard) and/or a subtle row style? I lean an explicit column
  (clear, sortable); no special coloring.

Yes, an explicit column is the right way to go.

- **S6-Q6 — Step status encoding.** Render the four step statuses as **palette
  color+glyph cells** in the row (like the Status table's Status column), not as
  clickable step buttons (science steps are not separately re-runnable from the
  Dashboard). Agree — cells, not buttons?

On the Science view, we want the same functionality as for Calibrations, i.e. the ability to (Re)Built.  These may need to be Intermediate files.

- **S6-Q7 — Deriving science on launch (no state file).** When there is no
  `*_state.json`, the model derives calibrations today; for science it would call
  `derive_science_from_disk(run_state, redux_dir, fitstbl)`. Include the science
  derive in Stage 6 (so a launch with no state file still shows science from the
  on-disk `spec2d`/`spec1d`), or rely on the state file for v1? I lean **include
  it** — it's the implemented path and matches the calib behavior; the model
  already builds a `PypeIt` for the calib derive, so `fitstbl` is available.

Yes, include the science derive in Stage 6.

- **S6-Q8 — CI-safe test fixture.** Add a synthesized science `*_state.json`
  fixture (a couple of frames — one science, one standard — with the four steps,
  a per-slit entry, and a couple of objects) to `pypeit/tests/files/`, and drive
  the view tests from it (mirroring the calib fixtures); the real `shane_kast_blue`
  science run is the interactive Sign off. Agree?

Yes, I agree.

- **S6-Q9 — Summary strip (R7).** Extend the global summary to include science
  (e.g. "Science: 3 frames · 2 extracted · 0 failed"), or keep the summary
  calibration-only and let the Science section carry its own count? I lean
  **extend the summary** so the state-first view reflects the whole run.

Extend the summary to include science.

*(Below: follow-ups **S6-Q10–S6-Q12** raised after the first round — the
per-object 1D viewer (from S6-Q3) and the science **(Re)Build** (from S6-Q6).)*

- **S6-Q10 — Per-object 1D viewing.** `pypeit_show_1dspec` selects an object by
  `--obj <name>` (e.g. `SPAT0424-SLIT0000-DET01`) or `--exten <N>`. The state's
  `ScienceObj` stores `objid`/`slitid`/`spat_pixpos`/`det` but **not** the
  `SPAT####-SLIT####-DET##` name or the FITS extension number. Options to launch
  the selected object: **(a)** reconstruct the `--obj` name from
  `spat_pixpos`/`slitid`/`det` (matches PypeIt's naming, but brittle if rounding
  differs); **(b)** add the object **name** (and/or `--exten`) to `ScienceObj`
  (a small `state.py` field addition, populated by the recorders/derive); **(c)**
  launch `pypeit_show_1dspec <spec1d> --list` / default and let the user pick in
  that GUI. I lean **(b)** — store the object name so the Dashboard can pass
  `--obj` reliably — but it is a state-model change; acceptable, or prefer (a)/(c)
  for v1?

  (a) should work.  Implement it.  

- **S6-Q11 — Science (Re)Build backend + observability.** The (Re)Build would run
  **`pypeit_reduce_by_step <pf> <frame> <step> --det`** (`step` ∈
  `process`/`findobj`/`extract`; `skysub` is folded into `findobj`), which writes
  the `Intermediate/` files. But — exactly like `run_to_calibstep` before the
  Stage-4 fix — **`reduce_by_step` does not pass `run_state`, so it writes no
  `*_state.json`**. For the (Re)Build to be **observable/live** and to update the
  Dashboard, I propose **mirroring the `run_to_calibstep` fix**: thread
  `run_state` through `reduce_by_step` (via the science recorders) + a final
  `derive_science_from_disk` write. That is a small pipeline change. Approve
  mirroring it, or for v1 just **re-derive science on completion** (no live
  during the science (Re)Build; `derive_science_from_disk` already reads the
  `Intermediate/` files)? I lean **mirror the fix** (consistent with calibrations
  and gives live science monitoring).

  Yes, mirror the fix.

- **S6-Q12 — Science (Re)Build clobber + lock semantics.** Reuse the **same
  `RunLock`** single-run lock (one PypeIt process at a time; the orange "Run in
  progress" cue). For clobber: a science step's products are the `Intermediate/`
  files (process→`sciImg`, findobj→`spec1d_*_all`+`Sky`, extract→`spec2d`+`spec1d`)
  — `reduce_by_step` overwrites them in place. Do we (a) just re-run (let
  `reduce_by_step` overwrite), with a confirmation naming what will be
  overwritten, or (b) crash-safe move-aside + restore (like the calib clobber)?
  And which **steps** get a (Re)Build control — all three per `(frame, det)`,
  gated on the preceding step's product existing (findobj needs `process`,
  extract needs `findobj`)? I lean: **confirmation naming the affected files** +
  the move-aside/restore safety (reuse the Stage-4 pattern), and **enable a step
  only when its prerequisite product exists**.

Implement (a) re-run with confirmation.  And enable a step only when its prerequisite product exists.

*(S6-Q1–S6-Q12 were resolved before implementation; the Mechanism/Deliverables/
Acceptance above reflect them.)*

*(Below: follow-ups **S6-Q13–S6-Q15** raised after the user's first sign-off pass
— the three **Round 1 Modifications**. Please answer inline under each **S6-Q**;
no code until they are resolved.)*

- **S6-Q13 — A graphical Science overview in the Status view (Mod 1).** Today the
  Status view's Science section is a one-line text count
  (`_build_science_placeholder`); the Calibrations section above it has the
  **navigator grid** (R17, `_build_navigator`/`_navigator_cell`): one
  colored+glyph cell per `(group, det)`, colored by the *worst* step status,
  clickable to scope. I lean to **mirror that navigator** for science: a compact
  grid of colored+glyph cells, **one per science `(frame, det)`**, each colored
  by `palette.worst_category` of the frame's four macro-steps
  (`process`/`findobj`/`skysub`/`extract`), the frame basename (+ `objtype`)
  under it, a tooltip listing the four step statuses, and clicking a cell
  **switches to the Science tab and selects that frame**. I'd keep the existing
  one-line count above the grid. Open choices:
    - **(a) Cell granularity** — one cell per `(frame, det)` (matches the
      Science-tab rows; kast is single-detector anyway) **[lean]**, or one cell
      per *frame* collapsing detectors to the worst across them?
    - **(b) What the cell encodes** — a single worst-of-four color+glyph (exactly
      like the calib navigator) **[lean]**, or a four-segment mini strip showing
      each step's status in the cell? The worst-of-four keeps it consistent with
      calibrations; the per-step strip is busier but more informative.
    - **(c) Click action** — switch to the Science tab and select the frame
      **[lean]**, or just scroll/no-op?
    - **(d) Many frames** — a multi-row wrap grid (science first, then
      standards), capped/scrollable if there are many? **[lean: wrap grid,
      science then standards, scroll if needed]**

  Is the navigator-style grid the right mirror, and do the leans (a)–(d) work?

Yes, I like the navigator-style grid.  Implement it.
(a) Let's do one cell per frame, det
(b) Let's try a four-segment mini strip showing each step's status.
(c) Let's switch to the Science tab and select the frame.
(d) Let's wrap the grid in a multi-row wrap grid, capped/scrollable if there are many.

- **S6-Q14 — The "red" default Science frame (Mod 2).** Diagnosed: the red is the
  Science table's **row-selection highlight**, not a status cell. On open, the
  Science tab auto-selects the first frame (`_build` →
  `_select_pair(self._rows[0])`), and a selected `QTableWidget` row is painted
  with the **desktop Qt theme's `Highlight` palette color** — which on your
  system renders **red** and reads as "failed." (In the offscreen screenshot the
  same selection renders *blue*, because offscreen uses the default palette — so
  it is theme-dependent, which is exactly the problem.) The step-status *cells*
  are only ever colored **text** (green ✓ success / white ○ undone / red ✗ only
  for a genuine `fail`), so they are not the culprit. Proposed fix: give the
  Science per-frame table an **explicit, theme-independent neutral selection
  style** (via a stylesheet `QTableWidget::item:selected`), so a selected frame
  reads as "selected," never "failed." Open choices:
    - **(a) Confirm the source** — does the red cover the **whole row** (i.e. the
      selection), as opposed to a single status cell? I'm confident it's the
      selection; please confirm so I fix the right thing.
    - **(b) Neutral selection color** — a soft **blue-grey** fill
      (`#CFD8DC` light / `#37474F` dark, normal text) **[lean]**, a plain light
      grey (`#E0E0E0`), or no row fill at all (just a magenta left-edge/border
      accent, echoing the Calibrations selected-step ring)?
    - **(c) Scope** — apply the same neutral selection style to the **other**
      selectable dashboard tables too (the per-object table here; and, for
      consistency, the Calibrations tables) **[lean: apply everywhere]**, or only
      to the Science per-frame table for now?

(a) yes, the red covers the whole row.
(b) Let's use a soft blue-grey fill.
(c) Let's apply the same neutral selection style to the other selectable dashboard tables too (the per-object table here; and, for consistency, the Calibrations tables).

- **S6-Q15 — Science QA PNGs (Mod 3).** Confirmed the on-disk science QA PNGs
  (dev-suite `shane_kast_blue`): under `<redux>/QA/PNGs/`, named
  `<basename>_<DETNAME>_S####_obj_prof.png` and `…_obj_trace.png` (per object/
  slit), plus flexure PNGs `<basename>_{global,local}_<DETNAME>_…_spec_flex_corr.png`
  and `…_spec_flex_sky.png`. They start with the **frame basename** and contain
  the **detector name**, so a `(frame, det)`'s QA files are
  `QA/PNGs/<frame>*<detname>*.png`. Unlike calibrations — whose `qa_files` are
  recorded into the state **live** (`calibrations.py` → `update_calib(...,
  'qa_files', ...)`) — the science state carries **no** `qa_files` field today.
  Two ways to expose them, reusing the Calibrations view's `_add_qa_files` +
  `QaImageDialog` (double-click a PNG → full view):
    - **(a) Derive at view-time in the model** — the model globs `QA/PNGs/` for
      the frame+detname and returns the list; **no state-model change**, works
      for both derived and live state, and mirrors the **S6-Q10(a)**
      reconstruct-from-disk choice you already approved. **[lean]**
    - **(b) Add a `qa_files` field to `ScienceFrameState`** populated by the
      recorders (live) + the derive path (from disk), mirroring the calibrations
      `qa_files` exactly (a small `state.py` change).

Let us go with (a)

  And two sub-choices regardless of (a)/(b):
    - **(c) Where to show them** — a **single QA list in the per-frame detail
      panel** (exactly like the Calibrations QA list), grouping all the frame's
      QA PNGs **[lean]**, or attach the per-object `obj_prof`/`obj_trace` PNGs to
      the **per-object table** rows?
    - **(d) Which PNGs** — **all** of them, including the flexure
      `spec_flex_*` PNGs (they all assess the science reduction) **[lean]**, or
      just `obj_prof`/`obj_trace`?

Let's show the per object PNGs, and all of them.

### Modifications

### Round 1

1. I would like have a more graphical view of the Science reductions in the Status view.  Something closer to the Calibrations.  *(Discussion + open choices in **S6-Q13**.)*

2. The default view of a Science frame in the Science view is for it to be highlighted red.  That gives the impression that the frame is failed.  I would like to change that to a more neutral color.  *(Diagnosis + fix proposal in **S6-Q14**.)*

3. As with the Calibrations, there are many QA PNG files for the Science frames assessing this part of the reduction.  Please expose these in a similar fashion to the Calibrations.  *(Discussion + open choices in **S6-Q15**.)*

**Round 1 — resolved + implemented (S6-Q13–Q15 answers folded in):**

1. **Science navigator in the Status view (S6-Q13).** The Status view's Science
   section gains a **navigator grid** mirroring the calibration navigator: one
   clickable cell per **`(frame, det)`** (S6-Q13a), each a **four-segment mini
   strip** showing the `process`/`findobj`/`skysub`/`extract` statuses as
   palette color+glyph (S6-Q13b), captioned with the (shortened) frame basename
   and a `⭑` tag for standards, with a tooltip listing the four statuses.
   Clicking a cell **switches to the Science tab and selects that frame**
   (S6-Q13c). The grid is a **multi-row wrap** (four cells wide), **science
   frames first then standards**, in a height-capped **scroll area** (S6-Q13d).
   The one-line count is kept above it.
2. **Neutral selection (S6-Q14).** Confirmed the "red" was the table's
   **row-selection highlight** inheriting the desktop theme's `Highlight` color
   (S6-Q14a). Fixed with an explicit **soft blue-grey** selection fill
   (`#CFD8DC` light / `#37474F` dark; `palette.selection_style`), applied to the
   Science per-frame and per-object tables **and** the Calibrations tables for
   consistency (S6-Q14b/c).
3. **Science QA PNGs (S6-Q15).** Exposed like the Calibrations QA list,
   **derived at view-time in the model** (`science_qa_files` /
   `science_object_qa_files` glob `QA/PNGs/<frame>*<detname>*.png`; no
   state-model change, S6-Q15a). The per-frame detail panel gains a **QA-files
   list of all of them** (`obj_prof`/`obj_trace` + flexure `spec_flex_*`;
   double-click → `QaImageDialog`, S6-Q15d), and the **per-object table** gains
   `obj_prof`/`obj_trace` columns that open that object's QA PNG (S6-Q15c).

## Docs

*(To be filled when Stage 6 is implemented — update `doc/dashboard/dashboard.rst`
(populate the Science-frames description; move it out of "Not yet implemented");
the design/coding docs + versions; note R15/R18 implemented.)*

## Prompts

### Generate this doc

1. Generate a new prompt document for Stage 6.  Call it dashboard_dev_stage_6.md.  Model it after this doc (many will be empty).  I will then edit it before proceding with the development.  Be sure to read all of the Context documents and the other prompt documents in this folder.

### Stage 6 — Science frames

1. I have edited this doc and answered the first set of S6-Q questions.  Please revise this doc and the design docs accordingly.  If you have additional questions, please raise them in the Clarifications section.  Do not generate code yet. Log your work.

2. I have answered the next round of questions.  Update this doc and the design docs accordingly.  If you have additional questions, please raise them in the Clarifications section.  Do not generate code yet. Log your work.

3. Please implement the Stage 6 changes.  Run tests to make sure everything is working.  Log your work.

4. Generate a set of Sign offs actions and add them to the Sign offs sub-section.  Log your work.

5. I started on the 2nd step of Sign Off and found a number of issues.  Please address the first set in Round 1 of the Modifications section.  Examine my requests and ask new questions in the Clarifications section.  Do not generate code yet. Log your work.

6. I have answered the questions related to Round 1 of the Modifications section.  Please update this doc and the design docs accordingly.  Please then implement them.  Log your work.

## Logging

The "Logs" section will record Claude's work.  Please use the following format:

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

...

## Logs

### 2026-06-15 (Generated the Stage 6 prompt document)

**Task.** Second task under *Prompts / Stage 6* (in `dashboard_dev_stage_5.md`)
— create this Stage 6 prompt document, modeled on the Stage 5 doc.

**Context read / grounding.** Built on the now-implemented science **state
layer** (`pypeit/state/run_state.py` + `science_status.py`; reviewed in the
Stage 6 prompt-1 audit): `get_science_status()` columns (`frame`, `detector`,
`objtype`, `process`, `findobj`, `skysub`, `extract`, `nobj`, `spec2d`,
`spec1d`), the per-frame `ScienceFrameState` with per-slit `ScienceSlit` +
per-object `ScienceObj` detail, and `derive_science_from_disk`. Confirmed the
product viewers `pypeit_show_2dspec <spec2d>` (+`--det`) and
`pypeit_show_1dspec <spec1d>`. Grounded the view in the design doc's
R15/R18 spec and the Calibrations-view drill-down pattern + the Stage 5
live-refresh.

**What I did.** Wrote `dashboard_dev_stage_6.md` mirroring the Stage 5 structure:
Goals (Stage 6 = the Qt Science view over the implemented state; the last
build-order stage), Claude/Skills, Coding, Context, a fleshed **Stage 6 —
Science frames** spec (design refs R15/R18/R8/R7; proposed model accessors +
`inspect.py` spec2d/spec1d builders + the per-frame table/detail view + summary
extension + the free Stage-5 live refresh; deliverables; acceptance criteria),
an **empty Sign offs** sub-section, a **Clarifications** channel with
**S6-Q1–S6-Q9** (where the view lives — third tab vs Status section; the
per-frame detail pattern; the product viewers; row granularity/grouping;
standards display; step-status-as-cells; deriving science on launch; the CI-safe
fixture; the summary-strip extension), empty **Modifications**/**Docs**
placeholders, a **Prompts** section, and this **Logging/Logs** section.

**Next step:** the user edits this doc and answers S6-Q1–S6-Q9, then the Stage 6
discussion/implementation prompt runs.

### 2026-06-15 (Stage 6 discussion: folded S6-Q answers; raised S6-Q10–Q12; no code)

**Task.** First task under *Prompts / Stage 6 — Science frames* — fold the user's
S6-Q1–Q9 answers into the plan + design docs, raise follow-ups. **No code.**

**Answers folded.** Rewrote the *Mechanism / Deliverables / Acceptance* for the
locked decisions: a **third Science tab** (S6-Q1) + a compact Status-view science
summary; the per-frame **detail panel** mirrors Calibrations (S6-Q2); **flat
`(frame, det)` rows** (S6-Q4) with an explicit **`objtype` column** (S6-Q5) and
the four steps as **palette color+glyph cells** (S6-Q6); **derive science on
launch** when no state file (S6-Q7); a synthesized science fixture for CI
(S6-Q8); the **summary strip extended** to span science (S6-Q9).

**Two answers expanded scope — investigated + raised follow-ups.**
- **S6-Q3 → per-object 1D.** `pypeit_show_1dspec` selects an object via
  `--obj <SPAT####-SLIT####-DET##>` / `--exten`, but `ScienceObj` stores
  objid/slitid/spat_pixpos/det, **not** that name/exten. Raised **S6-Q10** (store
  the object name in the state [lean] vs reconstruct vs `--list`).
- **S6-Q6 → science (Re)Build.** The user wants the Calibrations-style (Re)Build
  on the Science view. The backend is **`pypeit_reduce_by_step <pf> <frame>
  <step> --det`** (`step` ∈ process/findobj/extract; writes `Intermediate/`).
  Found it **does not pass `run_state`** — exactly the `run_to_calibstep` gap
  fixed in Stage 4 — so it writes no state. Raised **S6-Q11** (mirror the
  `run_to_calibstep` state-write fix for live/observable science (Re)Build [lean]
  vs re-derive-on-completion only) and **S6-Q12** (clobber/lock semantics: reuse
  `RunLock`; confirmation + move-aside/restore; enable a step only when its
  prerequisite product exists). Updated the **Scope note** accordingly (Stage 6
  now *does* launch per-step science (Re)Builds; full `run_pypeit` still only
  observed).

**Design docs.** Updated **R15** (per-object `pypeit_show_1dspec` + the science
(Re)Build via `pypeit_reduce_by_step`, lock-governed) and the coding-doc Stage 6
row; bumped design + coding to **v1.2.5**. No code; no tests run (docs only).

**Next step:** the user answers **S6-Q10–S6-Q12**, then the Stage 6
implementation prompt runs.

### 2026-06-15 (Stage 6 discussion round 2: S6-Q10–Q12 resolved; design locked; no code)

**Task.** Second task under *Prompts / Stage 6* — fold the S6-Q10–Q12 answers into
the plan + design docs; raise any remaining questions. **No code.**

**Resolved + folded.**
- **S6-Q10 (per-object 1D) → option (a).** Verified `SpecObj.set_name`
  (MultiSlit/IFU): the object name is
  `SPAT{SPAT_PIXPOS_ID:04d}-SLIT{SLITID:04d}-{DET}` (e.g.
  `SPAT0424-SLIT0000-DET01`). The per-object `spec1d_command` will **reconstruct**
  it as `SPAT{round(spat_pixpos):04d}-SLIT{slitid:04d}-{detname}` from the
  `ScienceObj` + the frame's `det` and launch `pypeit_show_1dspec <spec1d> --obj
  <name>` (with a `--list` fallback noted, since names come from
  `SPAT_PIXPOS_ID`).
- **S6-Q11 (Re)Build observability → mirror the fix.** `reduce_by_step` will get
  the same treatment as the Stage-4 `run_to_calibstep` fix: thread `run_state`
  through it (science recorders) + a final `derive_science_from_disk` write, so
  the science (Re)Build writes state live.
- **S6-Q12 (Re)Build clobber/lock → re-run with confirmation.** Reuse the
  `RunLock`; the clobber is a **re-run with a confirmation** naming the
  overwritten file(s) (no move-aside); a step is **enabled only when its
  prerequisite product exists** (process always; findobj needs process's
  product; extract needs findobj's).

**Updated.** Finalized the Stage-6 *Mechanism / Deliverables / Acceptance* (items
5–6 + the deliverables list, dropping the "open" caveats) and the design doc
**R15** (the resolved per-object viewer + the (Re)Build semantics +
`reduce_by_step` state-write fix). Bumped design + coding to **v1.2.6**. Marked
all S6-Q resolved — **no further questions**.

**Next step:** the Stage 6 **implementation** prompt — build the science
accessors + `inspect.py` builders + the `reduce_by_step` state-write fix + the
Science tab + the (Re)Build, with tests and screenshots.

### 2026-06-15 (Stage 6 implemented: the Science view + science (Re)Build)

**Task.** Third task under *Prompts / Stage 6* — implement Stage 6; run tests.

**Pipeline (`pypeit/`).**
- **`scripts/reduce_by_step.py`** — the state-write fix (mirrors the Stage-4
  `run_to_calibstep` fix): mark the chosen step `running` + `safe_write` at the
  start; `process` no longer returns early; at the end,
  `derive_science_from_disk` + `run_state.write()` so a science (Re)Build is
  observable/live and updates the Dashboard.
- **`state/run_state.py`** — `ScienceFrameState` gained **`raw_files`** (the
  contributing raw frame(s)); **`state/science_status.py`** `record_process`
  populates it from `fitstbl['filename'][frames]` (so the (Re)Build knows which
  raw frame to pass to `reduce_by_step`).

**Dashboard (`pypeit/dashboard/`).**
- **`model.py`** — `science_table()`, `has_science()`, `science_frame_entry()`,
  `science_slit_table()`, `science_object_table()`, `SCIENCE_COLUMNS`; and the
  **science-derive-on-launch** (`derive_science_from_disk`) added to the
  no-state-file derive path (S6-Q7).
- **`inspect.py`** — `spec2d_command`, `science_object_name`
  (`SPAT{round(spat)}-SLIT{slit}-{det}`, S6-Q10(a)), per-object `spec1d_command`
  (`--obj`), and `science_run_command` (`pypeit_reduce_by_step`, using
  `raw_files[0]`).
- **`view/science_view.py` (new)** — the third **Science tab**: a per-frame
  table (flat `(frame, det)` rows, `objtype` column, four step statuses as
  palette cells, nobj, spec2d/spec1d) + a detail panel (per-slit table,
  per-object table with **double-click → 1D**, **View spec2d**, and per-step
  **(Re)Build** — blue, lock-gated orange "Run in progress", prerequisite-gated).
- **`view/main_window.py`** — added the Science tab; wired the launcher/lock/
  refresh (the Stage-5 `_refresh_from_state` + `_on_lock_changed` now also drive
  the science view, so it auto-refreshes live).
- **`view/status_view.py`** — the placeholder became a **compact science
  summary** (counts), and the global summary strip now spans science (S6-Q9).

**Tests/fixture.** Added `pypeit/tests/files/dashboard_state_science.json`
(2 frames — science + standard — four steps, a per-slit, an object, `raw_files`)
and 5 science tests (model accessors, inspect builders incl. the reconstructed
`--obj`, the Science table+detail, the (Re)Build buttons + lock gating, the
third tab); updated the Stage-0 tab-bar test to the three tabs.
`pytest test_dashboard.py + test_state.py` → **85 passed**.

**Self-checked render.** `layout_check/dashboard_science_view.png` (also
`doc/figures/`) — the per-frame table, the per-slit/per-object detail, the blue
(Re)Build buttons, and the two-channel status bar all render.

**Docs.** `doc/dashboard/dashboard.rst` — new **Science view** section + figure,
the three-tab note, the compact-summary bullet, "Not yet implemented" trimmed;
**→ v1.3.0**. Design + coding docs → **v1.3.0** (R15/R18 implemented; build order
complete). A Bug-Fixes note for the `reduce_by_step` state write in
`doc/releases/2.1.0dev.rst`. `make htmlonly` → succeeded.

**Next step:** the user runs the Stage 6 Sign offs (esp. the interactive Science
(Re)Build + the per-object 1D viewer); the dashboard build order (Stages 0–6) is
then complete.

### 2026-06-15 (Stage 6 Sign offs generated)

**Task.** Fourth task under *Prompts / Stage 6* — generate the Sign offs and add
them to the Sign offs sub-section.

**What I did.** Wrote five Sign-off actions into the **Sign offs** sub-section,
mirroring the earlier stages: (1) the CI-safe `pytest` (the new science model/
inspect/view/tab tests); (2) review the `dashboard_science_view.png` screenshot
(third tab, per-frame table + objtype, the per-slit/per-object detail, View
spec2d + the (Re)Build buttons); (3) the **interactive launch** — the Science
tab on a real reduction, double-click an object → its 1D spectrum, View spec2d →
the 2D; (4) the **interactive science (Re)Build** — confirm dialog, the orange
run-lock cue, the live update on completion, and prerequisite gating; (5) the
**derive-on-launch** (no state file) + **live monitoring** during a run. No code
this turn.

**Next step:** the user runs the Sign offs; on acceptance, Stages 0–6 (the full
dashboard build order) are complete.

### 2026-06-15 (Round 1 Modifications: examined the 3 sign-off issues; raised S6-Q13–Q15; no code)

**Task.** Fifth task under *Prompts / Stage 6* — the user started sign-off step 2
and found issues; address the first set (**Round 1** Modifications), examine each
request, and raise new questions in **Clarifications**. **No code.**

**Grounding (read).** `view/science_view.py`, `view/status_view.py`,
`view/calibrations_view.py` (the navigator grid `_build_navigator`/
`_navigator_cell`; the QA list `_add_qa_files`/`_on_qa_double_clicked` +
`QaImageDialog`), `palette.py` (`worst_category`, `slit_style`, the LIGHT/DARK
colors), `state/science_status.py` + `state/run_state.py` (`ScienceFrameState`
has **no** `qa_files`; calib `qa_files` is recorded live in `calibrations.py`).
Viewed the rendered `doc/figures/dashboard_science_view.png`. Listed the on-disk
science QA PNGs in `$PYPEIT_DEV/REDUX_OUT/.../QA/PNGs/`.

**Findings per modification.**
- **Mod 1 (graphical Science in the Status view).** The Status view's Science
  section is a one-line count; the Calibrations section has the **navigator grid**
  (R17) the user is pointing at. The natural mirror is a science navigator: one
  colored+glyph cell per `(frame, det)`, colored by `worst_category` of the four
  macro-steps, clickable to jump to the Science tab. Open: cell granularity,
  worst-of-four vs per-step strip, click behavior, many-frames layout → **S6-Q13**.
- **Mod 2 (red default frame).** Diagnosed it as the per-frame table's **row-
  selection highlight**, which inherits the **desktop Qt theme's `Highlight`
  color** (red on the user's system; the offscreen screenshot renders it *blue* —
  hence theme-dependent). The step-status cells are colored *text* only (never a
  red row), so they are not the cause. Fix = an explicit theme-independent neutral
  selection style. Open: confirm the source, the neutral color, and whether to
  apply it to the other tables → **S6-Q14**.
- **Mod 3 (science QA PNGs).** Found the science QA PNG naming
  (`<basename>_<DETNAME>_S####_obj_prof.png`/`_obj_trace.png` + flexure
  `…_spec_flex_*.png`), all prefixed by the frame basename and containing the
  detector name → a `QA/PNGs/<frame>*<detname>*.png` glob. Science state has no
  `qa_files`. Proposed reusing the Calibrations QA list + `QaImageDialog`, and
  leaning to **derive the list at view-time in the model** (no state change,
  consistent with the S6-Q10(a) reconstruct-from-disk decision). Open:
  derive-vs-state-field, where to show, which PNGs → **S6-Q15**.

**What I changed (doc only).** Tagged each Round 1 item with its question
(`S6-Q13/14/15`) and added the three questions to **Clarifications** with my
diagnosis + leans. No code; no design-doc changes yet (deferred until the answers
lock, per the established pattern).

**Next step:** the user answers **S6-Q13–S6-Q15**; the next prompt folds them into
the plan + design docs and implements Round 1.