# Develop Stage 2 of the PypeIt Dashboard

## Goals

Continuing the staged build defined in `pypeit_dashboard_coding.md`
(*Developing → Build order*), this document guides **Stage 2 — the
Initialization / Status view**: the dashboard's landing, *state-first* view
that answers "where does this reduction stand?" at a glance.

Stage 2 builds on the artifacts already delivered:

- **Stage 0** (walking skeleton) — the `pypeit_dashboard` launcher, the
  `MainWindow` with the Status | Calibrations tab bar, and the shared header
  banner + logo. The Status tab is currently an empty placeholder.
- **Stage 1** (headless state layer) — `pypeit.dashboard.model.DashboardModel`
  (source-priority state acquisition, the normalized status table, the
  `(calib_id, det)` enumeration, the path-aware step order, edge-state
  reporting) and `pypeit.dashboard.palette` (the Qt-free status palette).

Stage 2 turns the Status tab placeholder into the real view by **consuming**
that model — it adds **no new PypeIt-facing logic**; the view is thin (per the
MVC split in the coding doc) and renders what the model provides.

Stages 0 and 1 are documented in `dashboard_dev_stages_0-1.md`; later stages
(Calibrations view, Execution/Locking, Monitoring, Science frames) get their own
documents.

Each stage below carries the design requirements it satisfies (the `R` items in
`pypeit_dashboard_design.md`), a concrete task breakdown, the files it touches,
and acceptance criteria. Genuinely open implementation choices are raised in the
**Clarifications** channel for the user to answer inline before coding begins.

## Claude

### Skills

Consider using the skills in PypeIt-development-suite/.claude/skills/
Especially:

- pyqt-expert
- pyqt6-ui-development-rules

## Coding

Here are guidelines for coding:

- Use Python where possible
- Add inline comments to explain the effort
- Reuse existing code when possible
- Place import statements at the top of the file.
- Include a description of inputs/outputs in the doc string of all methods
- Limit lines of code to 80 characters per line
- Add "Generated by JXP and Claude" in the doc string of all methods you create
- Production Dashboard code goes in `PypeIt/pypeit/dashboard/`
- Design-phase scratch/harness code (e.g. the layout-check harness) goes in
  `PypeIt-development-suite/pypeitdev/dashboard/py/`
- Follow the GUI conventions in `pypeit_dashboard_coding.md` (qtpy-only imports;
  `Signal`/`Slot`, not `pyqtSignal`/`pyqtSlot`)

## Context

Read the following documents to gain context:

- PypeIt-development-suite/pypeitdev/dashboard/pypeit_workflow.md
- PypeIt-development-suite/pypeitdev/dashboard/pypeit_dashboard_design.md
  (especially *Initialization* — R3, R7–R13, R15–R18 — and the *Default State
  View — visual design* section + the `dashboard_status_view.png` sketch)
- PypeIt-development-suite/pypeitdev/dashboard/pypeit_dashboard_coding.md
  (*Developing*, *Checking and modifying the layout*, *Debugging*)
- PypeIt-development-suite/claude_prompts/dashboard/dashboard_dev_stages_0-1.md
  (Stages 0–1: what is already built, and the S0/S1 resolutions this stage
  inherits)
- The Stage 1 model this view consumes: PypeIt/pypeit/dashboard/model.py
  (`DashboardModel`: `status_table()`, `calib_det_pairs()`, `step_order()`,
  `default_steps()`, `is_started()`, `load_status` + the `LOAD_*` constants) and
  PypeIt/pypeit/dashboard/palette.py (`step_style()`, `classify()`,
  light/dark color sets, glyphs)
- The Stage 0 view scaffolding: PypeIt/pypeit/dashboard/view/ (`main_window.py`,
  `header.py`, the `status_view.py` placeholder to replace)
- The setup GUI: PypeIt/pypeit/setup_gui/ (table/tree/tab widgets, the
  model/view patterns, and `dialog_helpers.py` to reuse)

## Development

This section defines Stage 2. Stage references (`R#`) point at
`pypeit_dashboard_design.md`; the stage table is in `pypeit_dashboard_coding.md`
(*Developing → Build order*).

### Stage 2 — Initialization / Status view

**Design refs:** R3 (color-coded state table), R7 (at-a-glance summary), R8
(grouped/ordered, scoped rows), R9 (required vs optional clarity), R10
(color + glyph, never color alone), R11 (graceful empty/edge states), R12
(manual refresh), R13 (stale-state indication), R15 (science-frame readiness —
designed-for, placeholder), R16 (scope drop-downs), R17 (configuration-overview
navigator grid), R18 (equally-prominent, scalable Science section — placeholder).

**Goal.** Replace the Status-tab placeholder with the real, *state-first*
landing view, rendering the Stage 1 `DashboardModel`. It reuses the
Dashboard-wide palette (light/dark) and stays thin — no PypeIt logic in the
widgets.

**Proposed layout** (top → bottom, per the design's *Default State View*; the
shared header banner from Stage 0 sits above all of this):

1. **Global summary strip** (R7) — whole-run health (e.g. "Calibrations: 6/6
   required success" + counts of fail / running / undone), always visible.
2. **Scope toolbar** (R16) — calibration-group and detector/mosaic drop-downs
   (from `model.calib_det_pairs()`) and a **Refresh** button (R12); a **stale**
   badge (R13) when the state file is older than the `.pypeit`/outputs. (A
   filter control is **deferred** to a later polish pass — S2-Q8.)
3. **Configuration-overview navigator** (R17) — a compact `(group × detector)`
   grid, each cell colored by the *worst* step status in that cell; clicking a
   cell sets the drop-downs (click-to-scope). A single cell for a
   single-group/single-detector run (e.g. Kast).
4. **Calibrations section** (R3/R8) — the scoped state **table** for the
   selected `(group, detector)`: columns **Step | Required | Status | Output**,
   each status as **color + glyph** (R10) from the palette, with required-vs-
   optional clarity (R9), step names in pipeline order (`model.step_order()`),
   file names monospace.
5. **Science frames section** (R15/R18) — equal-weight heading, a **placeholder**
   for now (the state model does not yet track science frames); designed so it
   can be populated later without redesign.

**Deliverables / tasks.**

1. **`StatusView` widget** (replace `view/status_view.py` placeholder) — takes a
   `DashboardModel` (+ theme), builds the layout above, and re-renders on scope
   change / refresh. Thin: all data via the model; all colors/glyphs via
   `palette`.
2. **State table** (R3, R8, R9, R10) — render `model.status_table()` filtered to
   the selected `(group, detector)`, ordered by `model.step_order()`, with the
   palette color + glyph per row and required/optional distinction.
3. **Global summary strip** (R7) — derive counts from the full
   `model.status_table()` (across all scopes), generalizing to science later.
4. **Scope drop-downs** (R16) — populate from `model.calib_det_pairs()`; changing
   either re-renders the table (and the navigator selection).
5. **Configuration-overview navigator grid** (R17) — `(group × detector)` cells
   colored by worst status; click-to-scope wiring to the drop-downs.
6. **Manual refresh** (R12) — re-acquire the state via the model and re-render.
   (The "source depends on whether a run is active" nuance and live updates are
   Stage 4/5; Stage 2 does a manual re-acquire.)
7. **Stale-state indicator** (R13) — flag when `*_state.json` is older than the
   `.pypeit` file or the calibration outputs.
8. **Empty/edge states (R11)** — render `model.load_status`
   (`not_started` / `malformed` / `file_not_found` / `error`) as a clear,
   centered message with a next action, not an empty grid.
9. **Science section placeholder** (R15/R18) — equal-weight heading + "tracked
   in a future stage" stub.
10. **Wire-in** — `app.launch` / `MainWindow` build a `DashboardModel` (which
    already holds `header_info`) and hand it to the `StatusView`.
11. **Theme-aware rendering** — use the palette's light/dark sets; verify via the
    render matrix.
12. **Layout-check harness extension + structural `pytest-qt` tests** — render
    the Status view across light/dark × default/resized with the fixtures
    (healthy / partial / failed / not-started) and assert structure (columns,
    drop-downs, summary, navigator). Produce milestone screenshots.

**Acceptance criteria.**

- With a healthy fixture state, the Status view shows the summary strip, the
  scope drop-downs (Kast: one group/detector), the navigator (single green
  cell), and the scoped table with correct color+glyph per step.
- Changing the scope drop-downs (or clicking a navigator cell) re-scopes the
  table; the summary strip stays whole-run.
- The four edge cases (`not_started`, `malformed`, `file_not_found`, a `fail`
  step) each render clearly (message or red ✗ row), no exceptions.
- Light and dark renders both legible; structural `pytest-qt` tests pass
  offscreen.

#### Sign offs

These are the actions for **you** to sign off on Stage 2. As with Stage 0, the
key human check is the **interactive GUI launch on your display**; the rest is
running tests and reviewing the rendered screenshots. Run from the PypeIt repo
root (`/home/xavier/Projects/PypeIt/PypeIt`).

1. **Automated tests pass (CI-safe).**
   - Execute: `python -m pytest pypeit/tests/test_dashboard.py -v`
   - Inspect: **23 passed** — Stage 0/1 tests plus the Stage 2 Status-view
     structural tests (`test_status_view_*`) and `test_palette_worst_category`.

All passed

2. **Review the milestone screenshots (Status view matrix).**
   - Inspect the PNGs in
     `$PYPEIT_DEV/pypeitdev/dashboard/py/layout_check/`:
     `dashboard_status_{healthy,failed,not_started}_{light,dark}_{default,resized}.png`.
   - Confirm: the **summary strip** (e.g. "7/7 required succeeded" for healthy,
     "3/6 … 1 failed … 2 to-do" for failed), the **scope drop-downs** (group /
     `DET01`), the **navigator** cell colored by worst status (green ✓ healthy,
     red ✗ failed), the scoped **table** (Step | Required | Status | Output) with
     color+glyph and dimmed optional rows, and the **Science** placeholder.
     Light **and** dark both legible; resized reflows cleanly. The
     `not_started` case shows the centered "not started" message (no table).
   - Regenerate with:
     `QT_QPA_PLATFORM=offscreen python $PYPEIT_DEV/pypeitdev/dashboard/py/dashboard_layout_check.py`

These PNGs all look fine

3. **Launch the GUI on your display — the interactive check.**
   - Execute (a reduction whose state file loads with the current schema; a
     freshly re-run `shane_kast_blue` 600/4310 works — see the Stage 1 caveat):
     `pypeit_dashboard $PF`
   - Inspect, on the **Status** tab: the summary strip, the group/detector
     drop-downs, the navigator cell, and the color+glyph table; change the
     drop-downs (multi-group/detector setups) and confirm the table re-scopes
     while the summary stays whole-run; click **Refresh** and confirm it
     re-reads/re-derives. The **Calibrations** tab is still a Stage-3
     placeholder.

The view is good.  When I click on **Refresh** it seems to reload the PypeIt file but it is not clear if it is reloading the state file.  Also, and more important, it would be good to have a status window that indicates the GUI is waiting on a job (if it is).  Consider adding this as part of Stage 3.  If you agree, update the design and coding docs to reflect this.

**→ Claude (Stage 2, prompt 3).** Confirmed and agreed.
- *Refresh behavior:* Refresh rebuilds the `DashboardModel`, which **does**
  reload `*_state.json` when present (R4) and only re-derives when it is absent
  (R5) — so it is reloading the state file; today there is just no on-screen
  feedback saying so. That feedback is exactly what the status/activity area
  provides.
- *Status/activity area:* this is the design's **X4/X5** (*Execution, Locking &
  Status*). I agree it should land sooner, so I **pulled X4/X5 forward from
  Stage 4 into Stage 3** in both docs: `pypeit_dashboard_coding.md` (build-order
  table → **v0.6**) and `pypeit_dashboard_design.md` (a Stage-3 scheduling note
  in *Dashboard status / activity area*, and X4/X5 will report what Refresh did
  — reloaded vs. re-derived — → **v0.11**). No Stage 2 code change; delivered in
  Stage 3.

4. **(Optional) Edge states.** Point the dashboard at a folder with no state
   file (not started) and at a setup with a pre-fix (nested-`input_files`) state
   file (malformed) and confirm each shows a clear message rather than crashing.

**Sign-off:** if items 1–3 look right, Stage 2 is accepted. Note anything off
(ideally the item + a screenshot) and Claude will address it.

JXP signs off at Sun Jun 14 01:10:32 AM UTC 2026

#### Clarifications

*(Discussion channel for Stage 2. Recommendations and open questions below —
please answer inline under each **S2-Q**. No code is written until these are
resolved.)*

- **S2-Q1 — State-table widget.** Use a simple `QTableWidget` for the scoped
  state table (one `(group, detector)` at a time → few rows), rather than a
  `QTableView` + `QAbstractTableModel` like `setup_gui`? I lean toward
  `QTableWidget` for Stage 2 (simpler, the table is small and re-rendered on
  scope change); we can graduate to a model/view later if needed.

Yes, start with a simple QTableWidget.

- **S2-Q2 — Navigator grid (R17) now or minimal?** Build the
  `(group × detector)` navigator as a general, click-to-scope grid now (it is a
  single cell for Kast but scales to MOS/mosaic heat-maps), or start with a
  minimal single-cell stub and generalize later? I lean toward building it
  generally now, since R17 is core to the state-first view.

Yes, build it generally now.

- **S2-Q3 — Launch wiring & deriving at startup.** Stage 2 should have
  `app.launch`/`MainWindow` construct a `DashboardModel` (it already carries
  `header_info`) and pass it to the view. At launch, if no state file is
  present, the model **derives** the state (R5), which needs RAW_DATA and may
  briefly block the UI (design says acceptable). OK to derive at launch, or
  should Stage 2 default to load-only and offer a "compute state" action?

Yes, derive at launch.

- **S2-Q4 — Stale-state heuristic (R13).** Flag "stale" when the
  `*_state.json` mtime is older than the `.pypeit` file or the newest
  `Calibrations/` output. Agree with that heuristic, and a non-blocking badge
  (not a blocking dialog)?

Yes, flag "stale" when the `*_state.json` mtime is older than the `.pypeit` file or the newest `Calibrations/` output.  A non-blocking badge is fine.

- **S2-Q5 — Refresh semantics (R12).** For Stage 2, **Refresh** re-instantiates
  the model (re-load the state file if present, else re-derive) and re-renders.
  The active-run-aware source selection and continuous live updates are deferred
  to Stage 4/5. Confirm.

I confirm.

- **S2-Q6 — Science section.** Scaffold the Science section as an
  equal-weight-heading **placeholder** only (populated in the later
  science-frames stage once `RunPypeItState` tracks it), per the S0/Stage-6
  decision. Confirm.

I confirm

- **S2-Q7 — Summary-strip content (R7).** Propose: "Calibrations: N/M required
  succeeded" plus small counts of fail / running / undone (optional steps noted
  separately). Good, or do you want a different headline?

That is fine for now.  We might edit it later.

- **S2-Q8 — Filter control.** Include a simple filter (e.g. *required only* /
  *failures only*) in Stage 2, or defer filtering to a later polish pass?

No, defer filtering to a later polish pass.

- **S2-Q9 — `det` formatting in the view (per S1-Q4/Q9).** The model leaves
  `det` raw (int or mosaic tuple); the view formats it. Use
  `spectrograph.get_det_name(det)` (→ `DET01`) for single detectors and a
  mosaic label (e.g. `MSC01` / `(1,5)`) for tuples, in the drop-downs and
  navigator? (The view can reach the spectrograph via the `.pypeit` file, or we
  expose it on the model.)

I only care above the view.  It should be easily interpretable by the user.

*(Below: follow-up questions **S2-Q10–S2-Q13** raised after your answers, from a
closer read of the built model + `spectrograph.get_det_name`. S2-Q1–S2-Q9 are
resolved; S2-Q8 = filtering deferred is folded into the spec above.)*

- **S2-Q10 — How the view gets readable `det` names (follow-up to S2-Q9).**
  PypeIt's canonical names come from `Spectrograph.get_det_name(det)` (→
  `DET01`; mosaics → `MSC01`), which needs the **spectrograph object** and a
  **tuple** for mosaics. The model already gives `det` as int/tuple
  (`status_table`/`calib_det_pairs`), but it does **not** currently expose the
  spectrograph. Cleanest: add a tiny `DashboardModel.det_name(det)` helper (the
  model holds the spectrograph it already parses, wraps `get_det_name`, with a
  plain readable fallback like `Det 1` / `Mosaic (1,5)` if naming fails), and the
  view just calls it. OK? (This keeps the *display* in the view while the one
  spectrograph touch stays in the model.)

You can and should take the spectrograph name from the PypeIt file.  Let me know if this is a problem.

- **S2-Q11 — Navigator "worst status" precedence (R17).** To color each
  `(group × detector)` cell by its worst step, I propose the precedence
  **fail > running > required-undone (to-do) > success**, with *optional* and
  *not-used* steps never worsening a cell (so an all-success-plus-skipped-optional
  cell still reads green). Agree?

Yes, that is fine.

- **S2-Q12 — "Not started" next action (R11).** The design's empty-state example
  has a *[Run reduction]* button, but launching a full `run_pypeit` is **deferred**
  (the coding doc limits v1 launching to `pypeit_run_to_calibstep`, Stage 4). So
  for Stage 2 the `not_started` (and `malformed`/`file_not_found`) states render
  as an **informational message only** — no actionable button yet. Confirm that's
  fine for now (the button arrives when launching is wired).

Yes, that's fine.

- **S2-Q13 — `MainWindow` constructor change (wire-in ripple).** Stage 0 built
  `DashboardMainWindow(header_info)`; Stage 2 needs it to take a
  `DashboardModel` (the header comes from `model.header_info`, the Status view
  gets the model). That changes the constructor and so updates the **Stage 0
  structural tests + the layout-check harness** that currently pass
  `header_info`. OK to make that refactor (Stage 0 behavior unchanged, just
  re-wired through the model)?

Yes, that sounds fine.

## Prompts

### Stage 2 — Initialization / Status view

1. I have answered your first set of S2-Q questions.  Examine the answers and write more as needed.  Do **not** write code yet. Log your work.
2. I have answered the additional S2-Q questions. Implement Stage 2 per the tasks and
   acceptance criteria: the `StatusView` (summary strip, scope drop-downs,
   configuration-overview navigator, scoped color+glyph state table, edge-state
   messages, Science placeholder), wired into the `MainWindow` via a
   `DashboardModel`; the layout-check harness extension and structural
   `pytest-qt` tests. Also generate a set of Sign offs actions and add them to
   the Sign offs sub-section. Produce the milestone screenshots (light/dark ×
   default/resized, across the fixtures) for my sign-off. Log your work.
3. I have signed off on all items except one.  See that section and act accordingly.  Log your work.

### Stage 3

1. Generate a new prompt document for Stage 3.  Call it dashboard_dev_stage_3.md.  Model it after this doc with similar sections (many will be empty).  I will then edit it before proceding with the development.  Be sure to read all of the Context documents and the other prompt documents in this folder.


## Logging

The "Logs" section will record Claude's work.  Please use the following format:

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

...

## Logs

### 2026-06-13 (Generated the Stage 2 prompt document)

**Task.** First task under *Prompts / Stage 2* (in `dashboard_dev_stages_0-1.md`)
— create this new Stage 2 prompt document, modeled on the Stages 0–1 doc.

**Context read.** `pypeit_dashboard_design.md` (Initialization R3/R7–R13/
R15–R18 + the *Default State View — visual design* section), `pypeit_workflow.md`,
`pypeit_dashboard_coding.md` (build order / layout-check / debugging), and the
sibling prompt docs (`dashboard_dev_stages_0-1.md`, `dashboard_coding_prep.md`).
Grounded the tasks in the **already-built** Stage 1 API — `DashboardModel`
(`status_table`, `calib_det_pairs`, `step_order`, `load_status`) and `palette`
(`step_style`, light/dark sets) — and the Stage 0 `view/` scaffolding.

**What I did.** Wrote `dashboard_dev_stage_2.md` mirroring the Stages 0–1
structure: Goals, Claude/Skills, Coding, Context, a fleshed **Stage 2 —
Initialization / Status view** spec (design refs, proposed layout, deliverables,
acceptance criteria), an **empty Sign offs** sub-section (filled at
implementation), a **Clarifications** channel with open questions **S2-Q1–S2-Q9**
(table widget, navigator scope, launch/derive wiring, stale heuristic, refresh
semantics, science placeholder, summary content, filter, `det` formatting), a
**Prompts** section, and this **Logging/Logs** section.

**Next step:** the user edits this doc and answers S2-Q1–S2-Q9, then the Stage 2
discussion prompt runs (no code until the questions are resolved).

### 2026-06-13 (Stage 2 discussion: reviewed S2-Q answers; raised S2-Q10–S2-Q13)

**Task.** First task under *Prompts / Stage 2 — Initialization / Status view* —
examine the user's answers to S2-Q1–S2-Q9 and write further questions as needed.
No code written.

**User answers (folded in).** S2-Q1 `QTableWidget` for the scoped table; S2-Q2
build the navigator generally now; S2-Q3 **derive at launch**; S2-Q4 stale =
`*_state.json` older than `.pypeit`/newest `Calibrations/` output, non-blocking
badge; S2-Q5 Refresh re-instantiates the model; S2-Q6 Science placeholder only;
S2-Q7 summary headline OK for now; **S2-Q8 filtering deferred** (removed from the
Stage 2 toolbar/spec); S2-Q9 format `det` in the view, must be easily readable.

**Grounding for the follow-ups.** Read `Spectrograph.get_det_name` — it needs the
**spectrograph object** and a **tuple** for mosaics; the model already yields
`det` as int/tuple but does not expose the spectrograph. That, plus the
launching-deferred and MainWindow-wiring implications, drove four new questions.

**Raised S2-Q10–S2-Q13.** S2-Q10 (add a `DashboardModel.det_name(det)` helper
wrapping `get_det_name` with a readable fallback, so the view just calls it);
S2-Q11 (navigator worst-status precedence: fail > running > to-do > success,
optional/not-used never worsen a cell); S2-Q12 (for now `not_started`/`malformed`
/`file_not_found` render as an informational message — no *[Run reduction]*
button, since full-run launching is deferred to Stage 4); S2-Q13 (change
`DashboardMainWindow(header_info)` → `DashboardMainWindow(model)`, updating the
Stage 0 structural tests + layout harness). Also updated the Scope-toolbar spec
to mark the filter deferred (S2-Q8).

**Next step:** await answers to S2-Q10–S2-Q13, then implement Stage 2 (the second
Stage 2 prompt).

### 2026-06-13 (Stage 2 implemented: the Status view)

**Task.** Second task under *Prompts / Stage 2* — implement Stage 2 per the
S2-Q answers, with Sign offs + milestone screenshots.

**S2-Q10–Q13 applied.** Model holds the spectrograph (parsed from the
`.pypeit`) and exposes `det_name(det)` (→ `DET01`/`MSC01`, readable fallback);
navigator worst-status precedence fail>running>to-do>success; edge states are
informational messages (no launch button yet); `DashboardMainWindow` now takes a
`DashboardModel`.

**Files.**
- `pypeit/dashboard/palette.py` — added `worst_category()` (severity precedence
  for the navigator, R17).
- `pypeit/dashboard/model.py` — `_parse_pypeit()` (returns HeaderInfo + spec),
  `DashboardModel` now stores `self.spectrograph`, adds `det_name()` and
  `is_stale()` (R13).
- `pypeit/dashboard/view/status_view.py` — the real **`StatusView`**: summary
  strip (R7), scope toolbar with group/detector drop-downs + stale badge +
  Refresh (R16/R13/R12), configuration-overview navigator grid (R17, click-to-
  scope), scoped color+glyph table Step|Required|Status|Output (R3/R8/R9/R10),
  Science placeholder (R15/R18), and centered edge-state messages (R11).
  Theme detected from the widget palette (light/dark).
- `pypeit/dashboard/view/main_window.py` + `app.py` — take/build a
  `DashboardModel` (derive at launch, S2-Q3); header from `model.header_info`
  with a minimal fallback when missing.
- `pypeitdev/dashboard/py/dashboard_layout_check.py` — now stages fixtures into a
  temp redux dir and renders the **Status view** across
  case(healthy/failed/not_started) × theme × size.

**Tests.** Updated the Stage 0 widget tests to pass a model; added Stage 2
structural `pytest-qt` tests (`test_status_view_*`: healthy table, scope
drop-downs incl. `DET01` formatting, failed row ✗, not-started/malformed edge
messages) and `test_palette_worst_category`. `pytest pypeit/tests/test_dashboard.py`
→ **23 passed**; dev-suite derive test still **1 passed**.

**Self-checked renders** (in `pypeitdev/dashboard/py/layout_check/`): healthy
(7/7, green ✓ navigator + table), failed (3/6, red ✗ navigator, ✗ fail row,
○ required rows, dimmed optional), not-started (centered message) — light+dark,
default+resized; all legible and reflow cleanly. Filled the **Sign offs**
sub-section and moved R3/R7–R10/R12/R13/R16/R17 to the design doc *Implemented*
bucket (R15/R18 placeholder noted).

**Note.** Real reductions only load once their `*_state.json` is regenerated
with the fixed writer (the Stage 1 nested-`input_files` caveat); the
fixtures/tests use schema-valid synthesized states.

**Next step:** the user runs the Stage 2 Sign offs; on acceptance, Stage 3 (the
Calibrations view).

### 2026-06-13 (Stage 2 sign-off: scheduled the status/activity area for Stage 3)

**Task.** Third task under *Prompts / Stage 2* — the user signed off on all items
except the item-3 note; act on it.

**The note (item 3).** The view is good; two points: (a) it's unclear whether
**Refresh** reloads the state file, and (b) more importantly, add a **status
window** showing when the GUI is waiting on a job — suggested for Stage 3, with
docs updated if I agree.

**What I did (no Stage 2 code change — this is forward-planning).**
- *Refresh clarification:* confirmed Refresh rebuilds the model, which **reloads
  `*_state.json` when present** (R4) and only re-derives when absent (R5); the
  gap is on-screen feedback, which the status/activity area will provide. Noted
  inline in the Sign offs.
- *Agreed and updated the docs* to pull the **Dashboard status/activity area
  (X4/X5) forward from Stage 4 into Stage 3**:
  - `pypeit_dashboard_coding.md` — *Developing → Build order* table: Stage 3 now
    includes X4/X5 (busy/waiting indicator; shows what Refresh/derive did),
    Stage 4 keeps X1–X3/C10; bumped to **v0.6**.
  - `pypeit_dashboard_design.md` — added a Stage-3 scheduling note to *Dashboard
    status / activity area* (and that X4/X5 will report reloaded-vs-re-derived on
    Refresh); bumped to **v0.11**.

**Outcome.** Stage 2 accepted; the one open item is resolved as a Stage-3
commitment (status/activity area), recorded in the design and coding docs.

**Next step:** Stage 3 (Calibrations view) — which will now also deliver the
Dashboard status/activity area (X4/X5).
