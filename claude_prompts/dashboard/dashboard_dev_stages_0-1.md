# Develop Stages 0 and 1 of the PypeIt Dashboard

## Goals

We will develop the PypeIt Dashboard in stages, following the build order
defined in `pypeit_dashboard_coding.md` (the *Developing* section). This document
guides the first two stages:

- **Stage 0 — Walking skeleton.** A launchable, but mostly empty, application,
  started **like every other PypeIt script** (the `pypeit_dashboard` console
  script, using the same `ScriptBase` pattern as `run_pypeit.py`): a required
  `.pypeit` launch argument, and a `MainWindow` with the tab bar
  (Status | Calibrations), the shared header banner, and the PypeIt logo —
  reusing the `setup_gui/` application/font/icon bootstrap. The two tab views are
  empty placeholders.
- **Stage 1 — State data layer (headless).** The Qt-free model layer that loads
  or derives the reduction state and exposes it as a clean, testable API
  (status DataFrame, `(calib_id, det)` enumeration, palette/glyph mapping,
  path-aware step order, edge-state handling). No Qt.

We build Stages 0 and 1 **together** (per the coding doc's "what is implemented
first"): a launchable skeleton plus the headless state layer. Stage 2 (the
Status view) consumes Stage 1's API and is handled in a subsequent document.

Each stage below carries the design requirements it satisfies (the `R` items in
`pypeit_dashboard_design.md`), a concrete task breakdown, the files it
touches, and acceptance criteria. Genuinely open implementation choices are
raised in the per-stage **Clarifications** channels for the user to answer
inline before coding begins.

## Claude

### Skills

Consider using the skills in PypeIt-development-suite/.claude/skills/
Especially:

- pyqt-expert
- pyqt6-ui-development-rules

## Coding

Here are guidelines for coding:

- Use Python where possible
- Add inline comments to explain the effort
- Reuse existing code when possible
- Place import statements at the top of the file.
- Include a description of inputs/outputs in the doc string of all methods
- Limit lines of code to 80 characters per line
- Add "Generated by JXP and Claude" in the doc string of all methods you create
- Production Dashboard code goes in `PypeIt/pypeit/dashboard/`
- Design-phase scratch/harness code (e.g. the layout-check harness) goes in
  `PypeIt-development-suite/pypeitdev/dashboard/py/`
- Follow the GUI conventions in `pypeit_dashboard_coding.md` (qtpy-only imports;
  `Signal`/`Slot`, not `pyqtSignal`/`pyqtSlot`)

## Context

Read the following documents to gain context:

- PypeIt-development-suite/pypeitdev/dashboard/pypeit_workflow.md
- PypeIt-development-suite/pypeitdev/dashboard/pypeit_dashboard_design.md
  (especially the *Initialization* section — R1–R18)
- PypeIt-development-suite/pypeitdev/dashboard/pypeit_dashboard_coding.md
  (especially *Developing*, *Checking and modifying the layout*, *Debugging*)
- The setup GUI: files in PypeIt/pypeit/setup_gui/ (the MVC pattern,
  `dialog_helpers.py`, `text_viewer.py`, and the application/font/icon bootstrap
  in `controller.py`'s `start_gui`)
- The state source: PypeIt/pypeit/state.py (`RunPypeItState`, `load()`,
  `get_status()`, the `(calib_id, det)` / step structure) and
  PypeIt/pypeit/scripts/pypeit_status.py (the no-processing derive path)
- The existing entry point: PypeIt/pypeit/scripts/pypeit_dashboard.py
  (`RunDashboard`, currently wrapping the reference prototype
  `pypeit/dashboard/dashboard.py` — reference only, not the basis for this build)

## Development

This section defines Stages 0 and 1. Stage references (`R#`) point at
`pypeit_dashboard_design.md`; the stage table itself is in
`pypeit_dashboard_coding.md` (*Developing → Build order*).

### Proposed package layout

The module layout for `PypeIt/pypeit/dashboard/`, mirroring `setup_gui/`'s
MVC organization (**approved**, S0-Q1).

```
pypeit/scripts/pypeit_dashboard.py   # RunDashboard(ScriptBase) — the launcher
                                     #   (like run_pypeit.py); main() calls app.py

pypeit/dashboard/
├── __init__.py
├── __main__.py          # optional convenience: `python -m pypeit.dashboard`
├── app.py               # application bootstrap (reuses setup_gui's)
├── model.py             # Stage 1: headless state/data layer (NO Qt)
├── palette.py           # Stage 1: Dashboard-wide status palette + glyphs
├── controller.py        # wiring + (later) subprocess launching
├── view/
│   ├── __init__.py
│   ├── main_window.py   # MainWindow: tab bar + shared header banner/logo
│   ├── header.py        # the shared header banner + logo widget (R6)
│   ├── status_view.py   # Stage 2 (placeholder in Stage 0)
│   └── calibrations_view.py  # Stage 3 (placeholder in Stage 0)
└── images/
    └── pypeit_logo.png  # logo asset shipped with the package (R6)
```

**Launch mechanism (S0 resolution).** The Dashboard is launched **exactly like
the other PypeIt command-line scripts**: through the existing
`pypeit/scripts/pypeit_dashboard.py` `RunDashboard(ScriptBase)` class, registered
as the `pypeit_dashboard` console entry point in `pyproject.toml` (the same
`ScriptBase.entry_point` pattern as `run_pypeit`). Its `main()` builds the
`QApplication` (via `app.py`, reusing the `setup_gui` bootstrap) and shows the
`MainWindow`. A thin `__main__.py` (`python -m pypeit.dashboard`) may be added as
a convenience but is **not** the primary launch path.

The reference prototype and associated files have been git-moved to
`pypeit/dashboard/prototype/` (S0-Q4); they are **not** carried forward and will
be **deleted later** once the new skeleton lands.

---

### Stage 0 — Walking skeleton

**Design refs:** R1 (launches via a Python script), R2 (launched from a folder
with a `.pypeit` file; the `.pypeit` file is a **required** launch argument),
R6 (shared header banner with `.pypeit` name, `PYP_SPEC`, setup/config ID,
reduction path, reduction directory, and the PypeIt logo top-right).

**Goal.** Produce a launchable, mostly empty application so the layout-review
process can exercise real widgets from day one. No reduction logic yet — the
views are placeholders and the header is populated from cheap, already-available
metadata (the `.pypeit` file + spectrograph), not from the state.

**Deliverables / tasks.**

1. **Entry point + argument parsing (R1, R2).**
   - Launch via the **`pypeit_dashboard` console script** — the
     `RunDashboard(ScriptBase)` class in `pypeit/scripts/pypeit_dashboard.py`,
     using the same `ScriptBase`/`entry_point` pattern as `run_pypeit.py`. Fix
     its `name()` (currently `'run_dashboard'`) to `'pypeit_dashboard'`; its
     `main()` launches the app (`app.py`). A `__main__.py`
     (`python -m pypeit.dashboard`) is an optional convenience, not the primary
     path.
   - The `.pypeit` file is a **required positional** argument (R2, S0-Q2),
     replacing the prototype's optional `-f/--pypeit_file`.
   - Keep `--redux_path` (testing aid) if useful.
2. **Application bootstrap (reuse `setup_gui`).** Reuse the
   application/font/window-icon/theming setup used to launch the setup GUI
   (`setup_gui/controller.py`'s `start_gui` path). Factor out a shared bootstrap
   only if low-friction; otherwise call/adapt it (S0-Q3).
3. **`MainWindow` shell.**
   - A tab bar with exactly two tabs: **Status** and **Calibrations**.
   - Each tab holds an empty placeholder widget (a labeled stub).
   - Default window size ≈ **1650×900** (the size used by the layout sketches).
4. **Shared header banner + logo (R6).** A `header.py` widget, shared by both
   tab views, showing the `.pypeit` file name, `PYP_SPEC` (spectrograph), the
   setup/configuration ID, the reduction **path** (MultiSlit / Echelle / IFU),
   and the reduction directory. The **PypeIt logo** sits in the **top-right
   corner**, clear of the banner text. Logo asset handling per S0-Q5.
5. **Loud failures (from the Debugging plan).** Install the global
   `sys.excepthook` (and the optional slot-wrapping decorator) now, so the very
   first interaction bugs surface with full tracebacks rather than being
   swallowed by the Qt event loop.
6. **Layout-check harness (Stage 0 slice).** A harness in
   `pypeitdev/dashboard/py/` that imports the real `MainWindow`, builds it with
   the `shane_kast_blue` 600/4310 example, and grabs it offscreen to a PNG across
   the light/dark × default/resized matrix (per the coding doc). Used for the
   self-check + milestone screenshot.
7. **Structural `pytest-qt` tests (written alongside).** Assert the structure:
   the tab bar is exactly `Status | Calibrations`; the header banner exposes the
   five R6 fields; the window constructs offscreen without error. Live in
   `PypeIt/pypeit/tests/` (CI-safe).

**Acceptance criteria.**

- `pypeit_dashboard <file>.pypeit` (and, if added, the optional
  `python -m pypeit.dashboard <file>.pypeit`) launches a window with the two tabs
  and a populated header banner + logo.
- A missing/omitted `.pypeit` positional argument is a clean argparse error (R2);
  the empty/edge handling of a *named-but-absent* file is shared with Stage 1
  (R11).
- The offscreen render matrix produces PNGs that match the header/tab layout in
  the design sketches; structural tests pass under `pytest-qt` offscreen.

#### Clarifications

*(Discussion channel for Stage 0. My recommendations and open questions are
below; please answer inline under each **S0-Q**. I have not written any code
yet.)*

- **S0-Q1 — Package layout.** Approve the proposed `pypeit/dashboard/` layout
  above (MVC mirroring `setup_gui/`, with `view/` as a sub-package)? Or do you
  prefer a flat `model.py`/`view.py`/`controller.py` trio exactly like
  `setup_gui/` (no `view/` sub-package)?

I approve the proposed layout.

- **S0-Q2 — Required `.pypeit` argument form.** R2 makes the `.pypeit` file a
  *required* launch argument. I recommend a **required positional** argument
  (`pypeit_dashboard shane_kast_blue_A.pypeit`), replacing the prototype's
  optional `-f/--pypeit_file`. Agree, or keep it as a (now *required*) `-f` flag?

Yes, require the .pypeit file as a positional argument.

- **S0-Q3 — Bootstrap reuse vs. refactor.** Reuse `setup_gui`'s app/font/icon
  bootstrap by (a) **calling/adapting** `start_gui`'s setup in place for now, or
  (b) **refactoring** the shared bootstrap into a common module both GUIs import?
  I lean toward (a) for Stage 0 (lowest friction) and refactoring later only if
  it proves worthwhile — consistent with the coding doc's "factor out only when
  both GUIs genuinely need it."

Let's go with (a) for now.

- **S0-Q4 — Fate of the prototype.** The reference prototype
  `pypeit/dashboard/dashboard.py` and `pypeit_worker.py` are not the basis for
  this build. Should I (a) **leave them in place untouched** (reference only),
  (b) **move them** to `pypeitdev/dashboard/py/` as historical reference, or
  (c) **delete them** once the new skeleton lands? (`pypeit_dashboard.py`'s
  `main()` import of the old module will be repointed regardless.)

I have git moved them into a prototype/ folder.  I suggest we delete them later

- **S0-Q5 — Logo asset location.** R6 puts the PypeIt logo in the header. The
  downscaled `pypeit_logo.png` currently lives in
  `pypeitdev/dashboard/images/` (design assets, not shipped). For production it
  must ship inside the package. I recommend committing a copy to
  `pypeit/dashboard/images/pypeit_logo.png` (as `setup_gui/images/` does for its
  icons). OK? Is there an existing packaged PypeIt logo I should use instead?

I approve the proposed location and suggestion.

- **S0-Q6 — Header "reduction path".** The MultiSlit/Echelle/IFU path is a
  property of the spectrograph class (`pypeit_file.pypeitpar`/the `Spectrograph`
  subclass). For Stage 0 I'll read it from the spectrograph derived from the
  `.pypeit` file (cheap, no reduction). Confirm that's the intended source for
  the header's path field, rather than waiting on the state layer.

Yes, that is the intended source for the header's path field.

---

### Stage 1 — State data layer (headless)

**Design refs:** R4 (if `*_state.json` is present, load it via
`RunPypeItState.load()`), R5 (otherwise derive it as `pypeit_status.py` does —
`PypeIt(calib_only=True).calib_all(status_only=True, reload_only=True)`; may
briefly block on launch), R11 (graceful empty/edge states: named `.pypeit` not
found; valid `.pypeit` but no state/outputs ("not started"); malformed/partial
state file).

**Goal.** A **Qt-free** model layer that is the single source of truth the views
consume. All PypeIt-facing logic lives here so it is unit-testable with plain
`pytest` (no display, and — for the load path — no RAW_DATA). The views stay
thin.

**Deliverables / tasks.**

1. **State acquisition with source priority (R4 → R5).** A model class
   (working name `DashboardModel`) that, given the `.pypeit` file and reduction
   directory:
   - loads `<pypeit_root>_state.json` via `RunPypeItState.load()` when present
     (fast path, R4); else
   - derives the state the `pypeit_status.py` way
     (`PypeIt(calib_only=True).calib_all(status_only=True, reload_only=True)`,
     reading `pypeIt.run_state`) — no processing performed (R5).
   - Records **which source** was used (for the stale-state / refresh logic in
     Stage 2's R12/R13).
2. **Status table.** Expose the existing `RunPypeItState.get_status()` DataFrame
   (columns: `calibration_group`, `detector`, `steps`, `required`, `status`,
   `output_file`) rather than re-deriving it. Add only thin convenience
   accessors the views need.
3. **`(calib_id, det)` enumeration.** Enumerate the calibration-group and
   detector/mosaic pairs present in the state, to populate the Stage 2 scope
   drop-downs and the Stage 3 selectors (R16/C2).
4. **Palette/glyph mapping (`palette.py`).** A Qt-free mapping from
   `(required, status)` (+ whether a step is in `default_steps()`) to the
   **Dashboard-wide status palette** color + glyph, exactly as tabulated in the
   design doc (success ✓ green, running ⏳ orange, fail ✗ red, required-undone ○
   white, optional – grey, n/a – dimmed grey). Returns plain data (hex + glyph +
   label), no `QColor`.
5. **Path-aware step order.** Expose the spectrograph's
   `Calibrations.default_steps()` order (MultiSlit/Echelle vs IFU), with `bpm`
   handling per the design (executed internally; omitted from the button row).
   This is consumed by the Stage 3 button row but lives in the model.
6. **Edge/empty states (R11).** Define explicit, typed results for: named
   `.pypeit` file not found; valid `.pypeit` but no state and no outputs ("not
   started"); malformed/partial `*_state.json`. The views render these as clear
   messages (Stage 2) rather than crashing.
7. **Tests + fixtures.** Headless `pytest` in `PypeIt/pypeit/tests/`, driven by
   committed `*_state.json` fixtures in `pypeit/tests/files/` (healthy
   `shane_kast_blue` 600/4310 + crafted edge cases: not-started, partial, one
   failed step, malformed). The **derive** path (R5) needs RAW_DATA, so its test
   runs in the dev suite (`unit_tests/`); the **load** path and all
   mapping/enumeration logic are CI-safe in the main repo (see S1-Q3).

**Acceptance criteria.**

- Given a fixture `*_state.json`, the model loads it and returns the status
  DataFrame, the `(calib_id, det)` list, and correct palette/glyph for each
  step — all without Qt and without RAW_DATA.
- The four edge cases each return a well-defined result the view can render
  (no exceptions escape the model).
- Palette/glyph output matches the design doc table exactly (verified by test).

#### Clarifications

*(Discussion channel for Stage 1. Recommendations and open questions below —
please answer inline under each **S1-Q**.)*

- **S1-Q1 — Model class shape.** One `DashboardModel` that owns the
  `RunPypeItState` and exposes the accessors above (status DataFrame, pairs,
  palette mapping, step order, edge state), or a thinner set of free functions?
  I lean toward a single class so Stage 2's controller has one object to hold and
  refresh.

- **S1-Q2 — Palette as data, not Qt.** Confirm `palette.py` returns **plain
  data** (hex strings + glyph + text label) and the *view* converts to `QColor`,
  keeping the model Qt-free and unit-testable. (A dark-theme-aware variant is
  noted in the design doc; for Stage 1 I'll define both light and dark hex sets
  as data so the view can pick — OK?)

- **S1-Q3 — Fixture set & placement.** Approve committing these `*_state.json`
  fixtures to `pypeit/tests/files/`: (1) **healthy** `shane_kast_blue` 600/4310,
  (2) **not-started**, (3) **partial**, (4) **one failed step**, (5)
  **malformed**? For the healthy one, may I generate it by running the
  `shane_kast_blue` 600/4310 reduction once (in the dev suite) and copying the
  produced `*_state.json`, then hand-crafting the edge cases from it?

- **S1-Q4 — `det` representation.** How should the detector/mosaic identifier be
  surfaced for the drop-downs/labels — the raw `det` value from the state
  (e.g. `1`, or a mosaic tuple), or a formatted name (`DET01`, mosaic label)? I
  can format in the model (single source of truth) or leave raw and format in
  the view; I lean toward formatting in the model.

- **S1-Q5 — Derive-path testing.** The R5 derive path
  (`PypeIt(calib_only=True)…`) requires RAW_DATA and a real reduction folder, so
  it can only be exercised in the dev suite. Confirm: cover the **load** path and
  all pure logic in `pypeit/tests/` (CI-safe), and add a single dev-suite
  `unit_tests/` test for the **derive** path against `shane_kast_blue` 600/4310?

- **S1-Q6 — `bpm` and not-in-pipeline steps in the model.** The model should
  report, per step, whether it is part of the active spectrograph's
  `default_steps()` (so the view can dim steps not used, and omit `bpm` from the
  button row). Confirm this path-awareness belongs in the model (I think it
  does), exposed as a per-step flag.

## Prompts

### Generate this doc

1. Generate a draft of this doc by editing it.  I have started a few sections, but you are encouraged to flesh it out as you see best. Examine the other prompts in this folder for guidance and also those in PypeIt-development-suite/claude_prompts.  I will then edit it before proceding with the development.

2.  Modify this doc (and the design/coding documents) to indicate the GUI will be launched like all other PypeIt scripts, e.g. `run_pypeit.py`.  If it is useful for you to use a __main__.py file for now, that is fine.  I have also answered the S0 questions.  Read those, and modify this doc and the other documents as needed.

### Stage 0 — Walking skeleton

1. Read this doc and the Context documents. Begin a conversation with me on
   Stage 0: confirm the scope, and **answer/raise the S0-Q questions** in the
   Stage 0 Clarifications channel. Do **not** write code yet. Log your work.
2. I have answered the S0-Q questions. Implement Stage 0 per the tasks and
   acceptance criteria: the entry point + required `.pypeit` argument, the
   `MainWindow` skeleton with the two tabs and the shared header banner + logo,
   the loud-failure hooks, the layout-check harness slice, and the structural
   `pytest-qt` tests. Produce the milestone screenshots (light/dark ×
   default/resized) for my sign-off. Log your work.

### Stage 1 — State data layer (headless)

1. Read this doc and the Context documents. Begin a conversation with me on
   Stage 1: confirm the model API and **answer/raise the S1-Q questions** in the
   Stage 1 Clarifications channel. Do **not** write code yet. Log your work.
2. I have answered the S1-Q questions. Implement Stage 1 per the tasks and
   acceptance criteria: the headless `DashboardModel` (load → derive source
   priority), the status-table accessor, the `(calib_id, det)` enumeration,
   `palette.py`, the path-aware step order, the edge-state handling, and the
   committed `*_state.json` fixtures + headless tests. Log your work.

## Logging

The "Logs" section will record Claude's work.  Please use the following format:

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>

...

## Logs

### 2026-06-13 (Set launch = standard PypeIt script; folded in S0 answers)

**Task.** Second task under *Prompts / Generate this doc* — make this document
(and the design and coding documents) state that the Dashboard launches **like
all other PypeIt scripts** (e.g. `run_pypeit.py`), and fold in the user's
answers to the S0-Q questions.

**Grounding read.** Confirmed the script pattern from
`pypeit/scripts/run_pypeit.py` (`RunPypeIt(ScriptBase)`) and its registration in
`pyproject.toml` (`run_pypeit = "pypeit.scripts.run_pypeit:RunPypeIt.entry_point"`).
The Dashboard already has a matching entry point
(`pypeit_dashboard = "pypeit.scripts.pypeit_dashboard:RunDashboard.entry_point"`)
and a stub `RunDashboard` whose `name()` currently returns `'run_dashboard'` and
whose `.pypeit` arg is an *optional* `-f/--pypeit_file`. The user has git-moved
the prototype (`dashboard.py`, `pypeit_worker.py`, `dashboard.rst`) into
`pypeit/dashboard/prototype/`.

**User's S0 answers folded in.** S0-Q1 layout **approved**; S0-Q2 `.pypeit` is a
**required positional** argument; S0-Q3 reuse `start_gui`'s bootstrap **in place
(option a)**; S0-Q4 prototype **moved to `prototype/`, delete later**; S0-Q5
**ship the logo at `pypeit/dashboard/images/pypeit_logo.png`**; S0-Q6 header
reduction **path comes from the spectrograph derived from the `.pypeit` file**.

**What I changed.**
- *This doc:* reframed the Stage 0 Goals bullet and the package-layout section to
  the standard-script launch (the `pypeit_dashboard` `ScriptBase` launcher in
  `pypeit/scripts/pypeit_dashboard.py`; `__main__.py` optional), marked the
  layout approved, recorded the prototype move/delete plan, and rewrote Stage 0
  task 1 + the acceptance criteria for the positional `.pypeit` argument and the
  `name()` fix (`'run_dashboard'` → `'pypeit_dashboard'`).
- *`pypeit_dashboard_design.md`:* updated *Initialization → How launch works* and
  R1 to specify the standard-script launch; bumped to **v0.10** with a changelog
  entry.
- *`pypeit_dashboard_coding.md`:* updated the Stage 0 row of the *Developing →
  Build order* table to the standard-script launch; bumped to **v0.5** with a
  changelog entry.

**Next step:** await the user's edits, then proceed to the Stage 0 implementation
prompt (the S0 questions are now resolved).
